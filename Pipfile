###############################################################################
#
# - Python Package Versions: don't overspecify them unless you need to!
#
#   Unlike requirements.txt, exact versions are kept stable by Pipfile.lock.
#   pipenv docs recommend defaulting to `*`, unless you know a newer version
#   will be incompatible with our code. Even then, its better to do e.g. `4.*`.
#
# - Where reasonable, split packages into sections using comments, explaining
#   who's using the dependency. This makes long-term maintenance much easier.
#
###############################################################################

[[source]]
name = "pypi"
url = "https://pypi.org/simple"
verify_ssl = true

[packages]
###############################################################################
#  Core packages: widely used packages
###############################################################################
boto3 = "*"
numpy = "*"
openai = "*"
###############################################################################
# LLM-guard integration
###############################################################################
# llm-guard = "*"

[dev-packages]
pytest = "*"
pytest-mock = "*"
coverage = "*"
ipython = "*"

[requires]
# Should match PYTHON_VERSION in:
# - docker/dockerfiles/Dockerfile
# - lib/cdo/pycall.rb
# - cookbooks/cdo-python/recipes/default.rb
python_version = "3.12"
