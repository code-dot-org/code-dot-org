SELECT user_id, level_id, script_id
FROM dashboard_production.user_levels
WHERE attempts > 0
  AND script_id = <%= unit_id %>
  <%= level_id && "AND level_id = #{level_id}" %>
<%= order && "ORDER BY id" %>
<%= limit && "LIMIT #{limit}" %>
;
