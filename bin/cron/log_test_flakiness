#!/usr/bin/env ruby
#require 'cdo/test_flakiness'
require_relative 'only_one'
require_relative '../../lib/cdo/test_flakiness'
require 'optparse'

require 'fileutils'
main do
  results = TestFlakiness.test_flakiness
  results.to_a.sort_by(&:last).map do |name, value|
    puts name
    puts value
    puts "-----"
  end
end

main
