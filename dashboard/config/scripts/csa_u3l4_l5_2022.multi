name 'CSA U3L4-L5_2022'
title 'Check for Understanding'

wrong 'wrong answer'
wrong 'wrong answer'
right 'right answer'
wrong 'wrong answer'
wrong 'wrong answer'

markdown <<MARKDOWN
What is the purpose of the following method?

```
public boolean mystery(int[] nums) {
  boolean flag = true;
  for (int index = 0; index < nums.length; index++) {
     if ((nums[index] % 2) == 1){
      flag = false;
    }
  }
  return flag;
}
```
MARKDOWN
