name 'CSA U5 Assessment q08_2023'

wrong '`BDA`'
right '`FHE`'
wrong '`HFE`'
wrong '`CEB`'
wrong '`FHI`'

markdown <<MARKDOWN
Consider the following code segment.

```
String[][] letters = {{"A", "B", "C"},
                      {"D", "E", "F”},
                      {"G", "H", "I"}};

System.out.println(letters[1][2] + letters[2][1] + letters[1][1]);
```

What is printed as a result of executing this code segment?
MARKDOWN

allow_multiple_attempts true
teacher_markdown <<MARKDOWN
`letters[1][2]` represents the element in the second row (index `1`) and the third column (index `2`) of the `letters` array, which is `"F"`.

`letters[2][1]` represents the element in the third row (index `2`) and the second column (index `1`) of the `letters` array, which is `"H"`.

`letters[1][1]` represents the element in the second row (index `1`) and the second column (index `1`) of the `letters` array, which is `"E"`.
MARKDOWN
