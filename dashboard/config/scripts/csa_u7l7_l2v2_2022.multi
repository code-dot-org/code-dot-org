name 'CSA U7L7-L2v2_2022'
title 'Check for Understanding'

right '`nums.size() == 0`'
wrong '`nums.get(0) + addItems(nums.subList(1, nums.size()))`'
wrong '`addItems(nums.subList(1, nums.size()))`'

markdown <<MARKDOWN
Consider the following code segment.

```
public static int addItems(List nums) {
  if (nums.size() == 0) {
    return 0;
  } else {
    return nums.get(0) + addItems(nums.subList(1, nums.size()));
  }
}
```

What is the base case?
MARKDOWN

allow_multiple_attempts true