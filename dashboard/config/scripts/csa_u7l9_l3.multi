name 'CSA U7L9-L3'
title 'Check for Understanding'

wrong '10'
wrong '12'
right '16'
wrong '26'
wrong '32'

markdown <<MARKDOWN
Consider the following recursive method.

```
public int addCall(int num) { 
   if (num <= 0) {
      return 0;
   }
   if (num == 1) {
      return 2;
   }
   return addCall(num - 1) + addCall(num - 2);
} 
```

What value is returned as a result of the call `addCall(6)`?
MARKDOWN

allow_multiple_attempts true