name 'CSD Prediction Header Size'
question 'Question'
wrong 'wrong answer'
right 'right answer'

markdown <<MARKDOWN
# Size of Headers

There are different sized headers which are marked by the number next to the h in the header tag. They go from &lt;h1&gt; to &lt;h6&gt; .


MARKDOWN
