name 'game_dev_predict_question'

wrong 'The Health variable increases by 1.', feedback: 'Incorrect. Look for the `if Health = 0` condition.'
wrong 'The Health variable decreases by 1.', feedback: 'Incorrect. Look for the `if Health = 0` condition.'
wrong 'Only the background will change.', feedback: 'Incorrect! There are three blocks inside the `if Health = 0` condition.'
right 'The background will change, the sprites will all stop, and the panda will say "Game Over!.', feedback: 'Correct. The code inside the `if Health = 0` condition will run.'
allow_multiple_attempts true
markdown <<MARKDOWN
##Predict what this code will do!

What will happen if the **variable Health equals 0**?
MARKDOWN
