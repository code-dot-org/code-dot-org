name 'CSA U6L3-L5v2_2022'
title 'Check for Understanding'

wrong '`population.length();`'
wrong '`population.total();`'
right '`population.size();`'
wrong '`size(population);`'
wrong '`length(population);`'

markdown <<MARKDOWN
Which of the following correctly returns the total number of elements in the `ArrayList` `population`?
MARKDOWN

allow_multiple_attempts true