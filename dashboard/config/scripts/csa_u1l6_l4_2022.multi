name 'CSA U1L6-L4_2022'
title 'Check for Understanding'

wrong 'I only'
wrong 'II only'
wrong 'III only'
wrong 'I and II only'
right 'I and III only'

markdown <<MARKDOWN
Which of the following are examples of inheritance?

I. `Plant` is the superclass, `Flower` is the subclass

II. `Car` is the superclass, `Engine` is the subclass

III. `Book` is the superclass, `GraphicNovel` is the subclass
MARKDOWN

allow_multiple_attempts true