name 'aif-y2-machine-learning-fundamentals-preassessment-q9-2026'

wrong 'The model might run more slowly than it would otherwise'
right 'The model may make inaccurate predictions for some kinds of inputs'
wrong 'The code used to train the model will be biased as well'
wrong 'The model will need more data before it is able to generate predictions'

allow_multiple_attempts false

markdown <<MARKDOWN
What is a consequence of training a machine learning model with biased data?
MARKDOWN

teacher_markdown <<MARKDOWN
Lesson 7: Selecting Data
MARKDOWN
