name 'CSA U1L5-L4 Placeholder_2022'
title 'Check for Understanding'

wrong 'I only'
right 'II only'
wrong 'III only'
wrong 'II and III'
wrong 'I, II, and III'

markdown <<MARKDOWN
What line of code should replace `/* missing code */` so that the code compiles?

```
Painter jes = new Painter();
if ( /* missing code */ ) {
   jes.move();
}
```

I. `canMove()`

II. `jes.canMove()`

III. `canMove(jes)`
MARKDOWN

allow_multiple_attempts true