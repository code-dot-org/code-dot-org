name 'CSA U1 Assessment q13_pilot-2025'

wrong 'II only'
wrong 'III only'
wrong 'I and IV'
right 'II and III'
wrong 'III and IV'

markdown <<MARKDOWN
Which of the following evaluates to `false`?

I. `true`

II. `!true`

III. `false`

IV. `!false`
MARKDOWN

teacher_markdown <<MARKDOWN
The `!` (NOT) operator negates the result of the condition. So `!true` will evaluate to `false`, while `!false` will evaluate to `true`.
MARKDOWN

allow_multiple_attempts true