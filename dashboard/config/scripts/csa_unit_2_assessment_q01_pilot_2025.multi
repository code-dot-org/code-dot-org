name 'CSA Unit 2 Assessment q01_pilot-2025'

wrong '`double size;`'
wrong '`String style = "high-top";`'
wrong '`boolean inStock = true;`'
right '`int price = 40.99;`'
wrong '`String material = "canvas";`'

markdown <<MARKDOWN
Which one of the following declarations are NOT correct?
MARKDOWN

teacher_markdown <<MARKDOWN
Variables of an `int` data type can only be assigned whole numbers. For decimal values, the variable should be a `double`.
MARKDOWN

allow_multiple_attempts true