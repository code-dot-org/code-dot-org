name 'CSPFinal_Question_9'
wrong 'At one of the lowest levels of abstraction, all digital data can be represented in binary using only combinations of the digits zero and one.'
wrong 'Hexadecimal is used to represent digital data because hexadecimal representation uses only 1 digit to represent groups of 4 binary digits.'
wrong 'Binary can be used to represent more complex, higher level abstractions, including but not limited to numbers, characters, and colors.'
right 'There is a limit to the number of things that can be represented in binary data at which point switching to hexadecimal representation is necessary. '

markdown <<MARKDOWN
Number bases, including binary, decimal, and hexadecimal, are used to view and represent digital data.  Which of the following is **NOT** true about representing digital data?
MARKDOWN
