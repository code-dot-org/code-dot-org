name 'U3L14 - MC Duplicate Ids'
title 'Buttons and Ids'
description ''
question 'What happens when two buttons are given the same id?'
wrong 'JavaScript is flexible. Two buttons with same id will respond to a single event handler associated with that id.'
right 'A warning message will appear for the second button that that id is already in use'
right 'The first button created with the id will work, the other will not'
wrong 'The JavaScript API for MVC allows for duplicate Ids as long as the HTML is CSS compliant'
wrong 'Two buttons with same id are linked during the running of the program.  Clicking on is just like clicking the other'
