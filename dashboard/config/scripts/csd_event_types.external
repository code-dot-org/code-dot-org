name 'CSD - Event Types'
href 'path/to/html/in/asset/folder'
markdown <<MARKDOWN
# Event Types

So far you have mostly used buttons which reach to click events. However as you begin using UI elements other than the button you might realize that you want the event handlers to listen for events other than click. The two most common event types you will use besides click are input and change.

## Change Event

How it works:

## Input Event

How it works: 
MARKDOWN
