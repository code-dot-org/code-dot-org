name 'CSA-SE-CV-Post-Assessment-Q01'

question 'Which of the following describes the role of AI in code generation?'

wrong 'AI writes the code entirely by itself without any human intervention.'
right 'AI provides suggestions for code completion, but the software engineer still makes the final decisions.'
wrong 'AI only helps in debugging, not in writing code.'
wrong 'AI has no role in code generation.'

allow_multiple_attempts false
