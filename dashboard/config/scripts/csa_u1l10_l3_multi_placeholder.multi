name 'CSA U1L10-L3 multi Placeholder'
title 'Check for Understanding'

markdown <<MARKDOWN
The `PainterPlus` class is a subclass of the `Painter` class.

Which of the following methods can be called on a `Painter` object?
MARKDOWN

wrong 'moveFast()'
right 'turnLeft()'
right 'canMove()'
wrong 'turnAround()'
allow_multiple_attempts true