name 'SP-VPL-CSA-22-mod3-L5-traversing-mc_2023'
title 'Check for Understanding'

wrong '2'
right '6'
wrong'9'
wrong'11'
wrong'13'


teacher_markdown <<MARKDOWN

MARKDOWN

markdown <<MARKDOWN
What is the value of `nums[2]` after running the program below?

```
int[] nums = {9, 10, 18, 12, 8};

for(int i = 0; i < nums.length; i++){
	if (nums[i] % nums[0] == 0){
		nums[i] = nums[i] / 3;
	} else {
		nums[i]++;
	}
}

```
MARKDOWN

allow_multiple_attempts true