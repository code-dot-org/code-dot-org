name 'CSA U4 Assessment q8'
height 'auto'

wrong ‘9.0’
wrong ‘-9.0’
wrong ‘27.0’
right ‘-27.0’
wrong ‘NaN’



teacher_markdown <<MARKDOWN
Covered in Unit 4 Lesson 2. 

MARKDOWN

markdown <<MARKDOWN
Consider the following code segment:

```
double a = 1.0;
double b = -3.0;
double output = Math.pow(b, Math.abs(b)) * Math.sqrt(a);
System.out.println(output);
```

What is printed when this code segment is run?


MARKDOWN

allow_multiple_attempts true