name 'CSA U6L7-L8 Placeholder'
title 'Check for Understanding'

markdown <<MARKDOWN
Consider the following code segment.

```
String originalTitle = "Once Upon a Time";
String newTitle = original.substring(0, 2) + original.substring(3, 5) + original.substring(12);
System.out.println(newTitle);
```

What is printed as a result of executing the code segment?
MARKDOWN

wrong 'Once Time'
wrong 'Once UTime'
wrong 'On e Time'
right 'One Time'
wrong 'One T'