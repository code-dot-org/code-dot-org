name 'CSA U3L4-L8_2023'
title 'Check for Understanding'

wrong '6'
wrong '5'
wrong '4'
right '3'
wrong '2'

markdown <<MARKDOWN
How many times will the body of this `for` loop execute?

```
for (int count = 6; count > 0; count -= 2) {
  System.out.println(count * 2);
}
```
MARKDOWN
