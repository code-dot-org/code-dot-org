name 'U3L25 Free Response 2'
title 'String and Substring: Assessment 2'

markdown <<MARKDOWN
What is the output of this code segment?

    var str = "Hello World";
	var worldIndex = str.indexOf("World");
	var answer = str.substring(worldIndex, worldIndex + "World".length);
	console.log(answer);

<br/>
MARKDOWN