name 'U2L07 Assessment3'
title 'Question 3'
question 'Which of the following is NOT something we are concerned with when we write an algorithm?'
right 'The algorithm is short.'
wrong 'The algorithm is correct.'
wrong 'The algorithm is efficient.'
wrong 'The algorithm is understandable.'

teacher_markdown <<MARKDOWN
Answer:

- A. The algorithm is short.
MARKDOWN

allow_multiple_attempts true