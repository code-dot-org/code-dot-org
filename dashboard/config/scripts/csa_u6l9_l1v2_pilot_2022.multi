name 'CSA U6L9-L1v2_pilot-2022'
title 'Check for Understanding'

wrong '`012345`'
right '`543210`'
wrong '`12345`'
wrong '`54321`'
wrong 'Nothing due to an infinite loop.'

markdown <<MARKDOWN
Consider the following code segment.

```
for (int index = 5; index >= 0; index--) {
   System.out.print(index);
}
```

What would be printed as a result of running this code?
MARKDOWN

allow_multiple_attempts true