name 'csp-7-exam1-testing2022'
editor_experiment ''
wrong ' `findMin(-1, 0)`  
`findMin(2,4)`  
`findMin(5,10)` '
wrong ' `findMin(5,3)`  
`findMin(7,2)`  
`findMin(5,1)` '
wrong ' `findMin(1,1)`  
`findMin(-2,2)`  
`findMin(0,3)` '
right ' `findMin(-1,1)`  
`findMin(1,-1)`  
`findMin(1,1)` '

markdown <<MARKDOWN
Which function calls would provide the most helpful test of this function? 

**Remember:** With tests, you are attempting to figure out all the possible ways the function could be broken.

```
function findMin(num1, num2){
	if(num1 < num2){
    	return num1;
    } else {
    	return num2;
    }
}
```

![](https://images.code.org/0aebd5ef2d083bc32fb1802c3bdb94e5-image-1581100792363.png)
MARKDOWN

teacher_markdown <<MARKDOWN
**Explanation:** The best set of tests for this function involves three different scenarios:  #1 first number is smaller, #2 second number is smaller, #3 both numbers are equal.

Students practiced testing library functions in  [Lesson 7 - Libraries Practice](https://studio.code.org/s/csp7-2021/lessons/7).

::: details [**Essential Knowledge Connected To This Question**]

* AAP-3.A.3: Parameters are input variables of a procedure. Arguments specify the values of the parameters when a procedure is called.

* AAP-3.B.5: Using parameters allows procedures to be generalized, enabling the procedures to be reused with a range of input values or arguments.

* AAP-3.A.7: The exam reference sheet provides the RETURN(expression) statement, which is used to return the flow of control to the point where the procedure was called and to return the value of expression.
:::



MARKDOWN

allow_multiple_attempts true