aws:
  access_key_id:      <%= ENV['AWS_ACCESS_KEY_ID'] %>
  secret_access_key:  <%= ENV['AWS_SECRET_ACCESS_KEY'] %>
  region:             <%= ENV['AWS_REGION'] %>

concurrency: 20  # The number of allocated threads to process messages. Default 25
queues:
  - <%= CDO.activity_queue %>
