{
  "category": "World",
  "config": {
    "name": "setPrompt",
    "expression": "setPrompt(QUESTION, 'VAR', function(val) {VAR = val;});",
    "inline": false,
    "blockText": "prompt user {QUESTION} for variable {VAR}",
    "args": [
      {
        "name": "QUESTION",
        "type": "String"
      },
      {
        "name": "VAR"
      }
    ]
  }
}