function testShowVariable(inputNumber, sizeDropdown, color) {
  	//put all of your code in here to make it persist when running
  	
  other.push(function() {
      clear();
      
    if(color == "blue"){
      fill("blue");
    }else{
      fill("red");
    }
      if(sizeDropdown == "large") {
       	textSize(100);
      } else {
       textSize(20);
      }
      text(inputNumber, 200, 200);
      //rect(0, 0, 100, 100);
    });
}
