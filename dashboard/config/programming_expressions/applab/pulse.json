{
  "key": "pulse",
  "name": "colorLeds[i].pulse()",
  "category": "Circuit",
  "category_key": "circuit",
  "content": "Makes the colorLed(s) pulse at a specific time interval. \r\n\r\nWhile similar to `.blink()`, the `.pulse()` method turns the colorLed(s) on and then slowly dims the light until the colorLed(s) turns off again. So when the light is pulsing, it's not just turning on and off, it's lowering its brightness.",
  "palette_params": [
    {
      "name": "interval",
      "type": "number",
      "required": true,
      "description": "The length of time (in milliseconds) that the light should be dimming for."
    }
  ],
  "parameters": [
    "interval"
  ],
  "short_description": "Makes the colorLed(s) pulse at a specific time interval. ",
  "syntax": "pulse(interval)"
}