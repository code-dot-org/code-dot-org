{
  "key": "noStroke",
  "name": "noStroke()",
  "category": "Drawing",
  "category_key": "drawing",
  "content": "Disables the stroke used for lines, points, and the border around shapes. Restore the stroke by using the `stroke()` command. For shapes, if both `noStroke()` and `noFill()` are run, nothing will be drawn to the screen.",
  "examples": [
    {
      "name": "",
      "description": "",
      "code": "```\n// Turn off the stroke and then back on again. Note the strokeWeight is maintained throughout.\r\nrect(100, 100, 200, 200);\r\nstrokeWeight(10);\r\npoint(150, 150);\r\nline(125, 250, 150, 250);\r\nnoStroke();\r\npoint(175, 150);\r\nstroke(\"red\");\r\nline(175, 250, 200, 250);\n```",
      "app": "",
      "image": null,
      "appDisplayType": "codeFromCodeField",
      "embedAppWithCodeHeight": 310
    }
  ],
  "return_value": "No return value. Changes future output to the display only.\r\n",
  "short_description": "Disables the stroke used for lines, points, and the border around shapes. Restore the stroke by using the stroke() command. For shapes, if both noStroke() and noFill() are run, nothing will be drawn to the screen.",
  "syntax": "noStroke"
}