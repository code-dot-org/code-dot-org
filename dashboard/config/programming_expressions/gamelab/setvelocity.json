{
  "key": "setVelocity",
  "name": "sprite.setVelocity()",
  "category": "Sprites",
  "category_key": "sprites",
  "content": "Sets the sprite velocity in both the x and y direction.\n\nA positive x parameter will move the sprite to the right, a positive y parameter will move the sprite downward. Must be used with the [`draw()`(#fff176)](/docs/gamelab/draw/) function containing a [`drawSprites()`(#fff176)](/docs/gamelab/drawSprites/). You can also update the velocity of a sprite using `setSpeedAndDirection()` or by updating the sprite properties [`sprite.velocityX`(#f78183)](/docs/gamelab/velocityX/) and [`sprite.velocityY`(#f78183)](/docs/gamelab/velocityY/). \n\nThe default velocity is 0 in both x and y directions.",
  "examples": [
    {
      "name": "",
      "description": "",
      "code": "```\nvar sprite = createSprite(200, 200);\r\nsprite.setVelocity(2,2);\r\nfunction draw() {\r\n  background(\"white\");\r\n  drawSprites();\r\n}\n```",
      "app": "",
      "image": null,
      "app_display_type": "codeFromCodeField",
      "embed_app_with_code_height": 310
    }
  ],
  "palette_params": [
    {
      "name": "x",
      "type": "Number",
      "required": true,
      "description": "The velocity of the sprite in the positive x direction, to the right."
    },
    {
      "name": "y",
      "type": "Number",
      "required": true,
      "description": "The velocity of the sprite in the positive y direction, downward."
    }
  ],
  "parameters": [
    "x",
    "y"
  ],
  "return_value": "No return value. Changes output in the display after drawSprites() is called.",
  "short_description": "Sets the sprite velocity in both the x and y direction.",
  "syntax": "sprite.setVelocity(x, y)",
  "tips": "- Changing [`World.frameRate`(#fff176)](/docs/gamelab/World.frameRate/) will affect the velocity."
}