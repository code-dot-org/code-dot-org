{
  "key": "contains",
  "name": "group.contains()",
  "category": "Groups",
  "category_key": "groups",
  "content": "Checks if the sprite is in the group.",
  "examples": [
    {
      "name": "",
      "description": "",
      "code": "```\n// Is the sprite in the group (red) or not?\r\nvar group = createGroup();\r\nvar sprite1 = createSprite(200, 200);\r\nif (randomNumber(0, 1)==1) group.add(sprite1);\r\nif (group.contains(sprite1)) {\r\n  group.setColorEach(\"red\");  \r\n}\r\nfunction draw() {\r\n  background(\"white\");\r\n  drawSprites();\r\n}\n```",
      "app": "",
      "image": null,
      "app_display_type": "codeFromCodeField",
      "embed_app_with_code_height": 310
    }
  ],
  "palette_params": [
    {
      "name": "sprite",
      "type": "Sprite",
      "required": true,
      "description": "The sprite you want to find in the group."
    }
  ],
  "return_value": "Boolean true or false.",
  "short_description": "Checks if the sprite is in the group.",
  "syntax": "contains(sprite)",
  "tips": "- Groups of sprites all have the same functions and you use the dot notation (combining the name of the group, followed by a dot, with the function name) to call the function for that group of sprites."
}