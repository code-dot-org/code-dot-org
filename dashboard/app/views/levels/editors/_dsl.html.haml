= render partial: 'levels/editors/fields/dsl_editors', locals: {f: f}
= render partial: 'levels/editors/fields/bubble_choice_sublevel', locals: {f: f} if @level.is_a?(LevelGroup)
= render partial: 'levels/editors/fields/audit_log', locals: {f: f}
