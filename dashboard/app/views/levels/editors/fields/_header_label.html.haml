.field
  = f.label :header_label, 'Header label'
  = f.text_field :header_label, placeholder: 'Story'
  %p.help-block Shown beside the unit's logo in the level header when the unit uses a banner instead of progress bubbles.
