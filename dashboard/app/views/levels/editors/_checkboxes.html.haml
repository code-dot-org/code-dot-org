- symbol = "#{symbol}".to_sym
- description = "#{description}"

//Field class is excluded so that additional tags/elements can be added under a checkbox
.aligned-input-group
  = boolean_check_box f, :"#{symbol}"
  = f.label :"#{symbol}", "#{description}"
