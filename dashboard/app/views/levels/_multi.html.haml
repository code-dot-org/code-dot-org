- content_for(:head) do
  =javascript_include_tag 'js/levels/multi'

= render partial: 'levels/single_multi', locals: {standalone: true, level: @level, last_attempt: @last_attempt, tight_layout: false }

= render partial: 'levels/common_audio'
