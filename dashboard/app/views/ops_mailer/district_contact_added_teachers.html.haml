#{@district_contact.ops_first_name} #{@district_contact.ops_last_name}
from #{@district_contact.districts_as_contact.first.try(:name)}
added #{@added_teachers.count} teachers to
%a{href: "cohort"} #{@cohort.name}:

%ul
  - @added_teachers.each do |teacher|
    %li
      #{teacher.ops_first_name}
      #{teacher.ops_last_name}
      = mail_to teacher.email
      - if teacher.district
        District: #{teacher.district.try(:name)}
      - if teacher.ops_school.present?
        School: #{teacher.ops_school}

= "[ops notification] #{@district_contact.ops_first_name} #{@district_contact.ops_last_name} added #{@added_teachers.count} teachers to #{@cohort.name}"
