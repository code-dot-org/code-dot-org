-@task = @plc_enrollment_task_assignment.plc_task

%p#notice= notice

%p
  This is the task assignment completion page for #{@task.name}

-unless @task.type == :plc_task
  %p
    Here is the subpage for actually completing the task with type #{@task.titleized_task_name}
  = render partial: 'plc/enrollment_task_assignments/complete/' + @task.underscore_task_name

= link_to 'Back', plc_enrollment_task_assignments_path
