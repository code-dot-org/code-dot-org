= form_for @plc_learning_module do |f|
  - if @plc_learning_module.errors.any?
    #error_explanation
      %h2= "#{pluralize(@plc_learning_module.errors.count, "error")} prohibited this plc_learning_module from being saved:"
      %ul
        - @plc_learning_module.errors.full_messages.each do |msg|
          %li= msg

  .field
    = f.label :name
    = f.text_field :name

  .actions
    = f.submit 'Save'
