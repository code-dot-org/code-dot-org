=form_for(@task, url: plc_task_path(@task)) do |f|
  .field
    = render partial: 'plc/tasks/edit/common_editable', locals: {f: f}
    = f.label :resource_url
    = f.text_field :resource_url

  .actions
    = f.submit 'Save'
