%h1 Listing plc_tasks

%table
  %thead
    %tr
      %th
      %th
      %th
      %th

  %tbody
    - @plc_tasks.each do |plc_task|
      %tr
        %td= plc_task.name
        %td= link_to 'Show', plc_task
        %td= link_to 'Edit', edit_plc_task_path(plc_task)
        %td= link_to 'Destroy', plc_task, :method => :delete, :data => { :confirm => 'Are you sure?' }

%br

= link_to 'New Task', new_plc_task_path
