= form_for @plc_course do |f|
  - if @plc_course.errors.any?
    #error_explanation
      %h2= "#{pluralize(@plc_course.errors.count, "error")} prohibited this plc_course from being saved:"
      %ul
        - @plc_course.errors.full_messages.each do |msg|
          %li= msg

  .field
    = f.label :name
    = f.text_field :name

  .actions
    = f.submit 'Save'
