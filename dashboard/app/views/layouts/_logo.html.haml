%div{id: 'main-logo'}
  %div{id: 'logo-container'}
    - if current_user
      = link_to(image_tag('logo.png'), '/home', {id: "logo-img"})
    - else
      = link_to(image_tag('logo.png'), CDO.code_org_url, {id: "logo-img"})
