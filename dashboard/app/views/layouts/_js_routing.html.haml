-# This file is generated by Webpack's SplitChunksPlugin,
-# which we only use when optimizing our webpack build (ie, non-development environments).
- if CDO.optimize_webpack_assets
  %script{src: webpack_asset_path('js/studioRoutes.js'), 'data-ot-ignore' => ''}

%script{src: webpack_asset_path('js/routing.js'), 'data-ot-ignore' => '', 'data-config' => {
  default_url_options: default_url_options.merge(script_name: request.script_name),
}.to_json}
