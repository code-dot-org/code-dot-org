= form_for @professional_learning_course do |f|
  - if @professional_learning_course.errors.any?
    #error_explanation
      %h2= "#{pluralize(@professional_learning_course.errors.count, "error")} prohibited this professional_learning_course from being saved:"
      %ul
        - @professional_learning_course.errors.full_messages.each do |msg|
          %li= msg

  .field
    = f.label :name
    = f.text_field :name

  .actions
    = f.submit 'Save'
