:css
  body {
    font-family: Metropolis, sans-serif;
  }

%p
  Hi
  = @workshop.organizer.name.split().first.titleize + ','

%p
  = @enrollment.full_name
  signed up for your
  - if @workshop.course == Pd::Workshop::COURSE_BUILD_YOUR_OWN
    = @workshop.name.presence || Pd::Workshop::COURSE_BUILD_YOUR_OWN
  - else
    = @workshop.course
    = @workshop.subject
  = link_to 'workshop', @workshop.workshop_dashboard_url
  on
  = @workshop.sessions.first.formatted_date
  at
  = "#{@workshop.location_name}."

= render partial: 'teacher_info'

%p
  To view and manage enrolled teachers, access your workshop through the
  =link_to 'workshop dashboard.', @workshop.workshop_dashboard_url

%p
  Thanks!
