// DEPRECATD: Do not add new javascript dependencies here. Instead, you should
// add them to the apps package so they can be explicitly required/imported where
// they are needed.

// This is a manifest file that'll be compiled into application.js, which will include all the files
// listed below.
//
// It's not advisable to add code directly here, but if you do, it'll appear at the bottom of the
// compiled file.
//
// Read Sprockets README (https://github.com/sstephenson/sprockets#sprockets-directives) for details
// about supported directives.

// Important: Use minified versions of dependencies. No minifier is in use today.
// Load order is deliberate and grouped by where each file comes from.

// Pre-minified, gem-supplied asset. `bundle update` can affect the size of
// these, which could trigger the size cap in
// test/lib/application_js_bundle_test.rb without touching this file.
//= require jquery.min

// Pre-minified, vendored asset in vendor/assets/javascripts. Updated manually.
// jquery-ui.min is the official 1.12.1 full build from
// https://code.jquery.com/ui/1.12.1/jquery-ui.min.js.
//= require jquery-ui.min

// Pre-minified, vendored assets in vendor/assets/javascripts. Updated manually.
//= require jquery.mask.min
//= require jquery.qtip.min
//= require jquery.ui.touch-punch.min

// Small unminified glue, ~50KB raw in total. This list is frozen: changes are
// limited to security updates of what is already here. New frontend code
// belongs in apps/ or frontend/, not in this bundle.
// jquery_ujs is gem-supplied, and the rest are vendored.
//= require jquery_ujs
//= require jquery.placeholder
//= require jquery.simulate
//= require jquery.overlaps

// window.dashboard is used to communicate between dashboard/apps in a variety
// of places. In this case we want to make availale to our JS the url of the
// pegasus portion of our site (i.e. http://code.org vs. http://studio.code.org)
window.dashboard = window.dashboard || {};
window.dashboard.CODE_ORG_URL = "<%= CDO.code_org_url %>";
window.dashboard.rack_env = "<%= CDO.rack_env %>";
