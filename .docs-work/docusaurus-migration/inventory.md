# Docusaurus migration inventory

## Current Starlight site (`docs/site/`)
- Astro 7.3.2 + Starlight 0.42, Node 22 (.nvmrc), npm, standalone (not in frontend/ workspace)
- Content via symlinks: `src/content/docs/{students,teachers,district-administrators,developers}` -> `../../../../<audience>`, plus `index.md`
- Autogenerated sidebar per audience root; Pagefind search; no React integration
- Custom frontmatter: `type` (concept|task|reference|troubleshooting|tutorial) validated via Zod
- Scripts: `evidence-check.mjs` (35 lines, AJV + parity), `affected-docs.sh` (26 lines, git diff + jq)
- Build output: `docs/site/dist/`; preview daemon running on PID 1154467 and 2280864
- .gitignore: `docs/site/node_modules/`, `docs/site/dist/`, `docs/site/.astro/`

## Content
- 159 total .md pages across 4 audience roots (students 48, teachers 38, district-admin 6, developers 47) + index.md + loose legacy
- Frontmatter: exactly `title`, `description`, `type`
- Internal links: absolute site paths with trailing slashes (`/teachers/classes/create-a-section/`)
- Images: relative `images/<slug>-<name>.png` beside pages; 13 image directories
- Deepest: 3 levels (e.g. `students/getting-started/create-an-account.md`)
- Legacy exclusions: `docs/*.md` (10+ loose files), `docs/plc/`

## Design system (source of truth for branding)
- Fonts package: `@code-dot-org/fonts` - Geist (primary), Space Grotesk (headings), Noto Sans (fallback)
  - Loaded via `@fontsource/geist` SCSS mixins; studio imports `@code-dot-org/fonts/brands/code.org/index.css`
- Tokens: `@code-dot-org/component-library-styles` CSS custom properties
  - `fontVariables.css`: `--font-family-main` (Geist stack), `--font-family-heading` (Space Grotesk stack)
  - `primitiveColors.css`: `--brand-purple-50: #9657c7`, `--brand-aqua-50: #3cfff7`
  - `colors.css`: semantic tokens keyed to Light/Dark themes
- MUI theme: `CdoTheme` at `component-library/src/themes/code.org/index.ts` (createTheme)
- Studio app pattern: imports font CSS + 5 token stylesheets, wraps in `ThemeProvider`
- Logo: `logo-codeai-inverse.svg` (white text, for dark backgrounds), `code-org-logo.png` (86x85 icon)
- All workspace-only (`workspace:*`); not published to npm

## Documentation about docs
- `docs/README.md`: references `docs/site/`, nvm 22, npm commands
- `docs/developers/documentation/*.md`: 3 files reference `docs/site`, `nvm use`, `npm run`, "Astro/Starlight"
- `.docs-evidence/README.md`: references `npm run evidence:check` in `docs/site`
- `.docs-work/platform.md`: describes Starlight setup

## Target: `frontend/apps/docs/`
- Workspace: `frontend/package.json` includes `apps/*`; Node >=20; yarn 4; turbo tasks
- Turbo: `build` task with `dependsOn: ["^build"]`, `outputs: ["dist/**"]`
- Existing apps extend `@code-dot-org/lint-config/typescript/tsconfig.vite.app.json`
- Docusaurus uses its own TS config; will extend minimally
