# Orchestrator decision log

Working notes for the documentation project. Not documentation. Newest last.

## 2026-09-09 Phase 0

- Baseline revision: `9793f8d36ae` on `staging`. Local app: http://localhost-studio.code.org:3000 (Puma, healthy). Browser verification runs against this host only. Never test-studio, staging, or production.
- Accounts: local student and teacher accounts may be created. `/api/test/create_user` is routed in development and test only (`dashboard/config/routes.rb`, `rack_env?(:development, :test)`). The e2e helper is `frontend/packages/e2e-tests/tests/shared/auth.ts` (`createUser`). Local DB at start: 139 teachers, 202 students.
- Models: main agent Fable. Opus workers requested as `claude-opus-4-6[1m]`; the `opus` alias in this session resolves to `claude-opus-5` (checked in the subagent transcript) and agent definitions are not hot-loaded. User accepted the fallback. Pinned definitions exist at `~/.claude/agents/docs-opus.md` and `docs-sonnet.md` for a future session.
- Usage guard: every agent runs `~/.claude/skills/usage-guard/check-usage.sh` at start and end; threshold 90 on any window; breach is reported upward as `USAGE BREACH`.
- Product name: **CodeAI**. The company and platform are "CodeAI (formerly Code.org)". Write "code.org" only inside URLs and email addresses. The site is studio.code.org. Evidence: `apps/i18n/common/en_us.json` (169 CodeAI vs 5 Code.org), tutor prompts in `dashboard/app/helpers/aidiff_prompt_helper.rb`, header alt text "CodeAI Home" observed in the browser.
- House style: Agent Stylebooks (Neeeophytee/agent-stylebooks, MIT) installed via `npx skills` into `.agents/skills/`, the repo's existing skill mechanism. Mapping:
  - Student, Teacher, District admin pages: `github-docs` primary; `govuk` for plain-language and decision pages; `microsoft-writing-style` for UI-text and support tone.
  - Engineering and product docs: `gitlab-docs` (Concept / Task / Reference / Troubleshooting).
  - Setup, how-to, onboarding: `google-developer-docs`.
  - Technical explanation and reference: `mdn-web-docs`.
  - Where a stylebook conflicts with its source guide or with this product, the source guide and the product win.
- Evidence grades used everywhere: VERIFIED (browser), OBSERVED (rails runner, db, curl), STRONGLY SUPPORTED (code + tests), INFERRED, AMBIGUOUS, BLOCKED, CONTRADICTED.
- Environment quirks seen in the browser: OneTrust and GTM blocked by the sandbox certificate (harmless); `/shared/images/courses/logo_tall_oceans.jpg` returns 500 locally (pegasus asset, not product behavior).
- Stylebook install result: six skills under `.agents/skills/<name>/` (SKILL.md + agents/openai.yaml + references/provenance.md), six entries added to `skills-lock.json`, nothing else touched. Five sources are openly licensed (CC BY / CC BY-SA / OGL); `microsoft-writing-style` is a reference-only interpretation, so treat it as tone guidance, never as quotable text. Open question for the user: vendor the six directories, or gitignore them like `playwright-best-practices` and restore with `npx skills experimental_install`.
- Renderer constraint: Astro 7.3 requires Node >= 22.12 and Starlight 0.42 depends on Astro 7. The repo default Node is 20.20.2 but nvm already has 22.12.0. A docs-site package would carry its own `.nvmrc`/`engines`. Decision deferred to after the inventory.
- Model pin resolved: after the user reloaded plugins, `docs-opus` (claude-opus-4-6[1m]) and `docs-sonnet` are live; a probe confirmed the transcript model is `claude-opus-4-6` and that a docs-opus agent can spawn docs-sonnet children. The Phase 1 inventory agent had already started on `claude-opus-5` and runs to completion; every later Opus worker uses `docs-opus`.
- Concurrency cap (user, 2026-09-09): at most 3 subagents running at once, counting Sonnet children. Domain owners run in waves of <= 3 and are told to run their own Sonnet helpers sequentially, one at a time. Fable checks usage before each wave.
- Server policy (user, 2026-09-09): the dashboard server on http://localhost-studio.code.org:3000 is long-running and shared. No agent starts, stops, restarts, or spawns another Rails, Puma, webpack, or rspack process. If the server is unreachable, mark the journey BLOCKED (environment) and report to Fable; Fable tells the user.
- Wave 0 done (platform, two Sonnet sweeps → `.docs-work/raw/oq-*.md`, `.docs-work/platform.md`). Screenshot helper changed to viewport-only by default and to hide `#environment_tag`. Wave 1 launched 2026-09-09: classrooms-and-progress, accounts-and-access, curriculum-and-assignment; no Sonnet helpers during waves so the in-flight count stays at 3.
- 2026-09-09 18:15 PT: accounts-and-access done (24 pages, 4 journeys green, OAuth/LTI/password-reset journeys BLOCKED by local credentials and reCAPTCHA). Downgrade guard: the HTTP endpoint blocks a teacher with real sections (`can_change_own_user_type?`); the older memory note is CONTRADICTED for the endpoint. Session usage hit 87%; wave 2 waits for the 7:59pm PT reset. Terminology conflict queued: accounts pages say "class", classrooms draft says "section", UI sign-in field says "Section code"; resolve after classrooms reports.
- 18:30 PT: curriculum-and-assignment done (15 pages, 3 journeys green, 2 screenshots). published_state matrix: in_development = levelbuilder only; pilot = levelbuilder + pilot teachers + their students; beta..deprecated = anyone by URL; catalog lists preview/stable with student participant audience. Owner votes "section" as the primary UI term. Session 89%.
- 18:35 PT: classrooms-and-progress done (22 pages, 4 journeys green, give-feedback BLOCKED pending a real submission, 1 screenshot). Wave 1 total: 61 pages.
- Terminology ruling (Fable): **"section"** is the product noun everywhere (UI: "Create a section", "Section code", sidebar, join flow). "Class" may appear only as the ordinary word for the real-world group of students ("share the code with your class"), never as the name of the product object. Queued: sweep the 24 accounts pages (they say "class code"), then unify cross-domain link paths (`teachers/classes/` dir name stays; the "classes" directory is a task group label, acceptable). Integration review after the usage reset.
- 18:35 PT user review of the wave 1 site: (1) prose is not cohesive or enjoyable, below the gold-standard bar; (2) needs more screenshots; (3) internal links broke when hot-linked. Fable actions: rewrote 50 internal links in 30 pages to canonical site paths (`/audience/group/page/`); remaining dangling targets point at future pages: `/district-administrators/integration/lms-setup/`, `/district-administrators/lms-integration/`, `/teachers/ai/evaluation/`, `/teachers/classes/choose-a-login-type/` (unify these when the owning waves land). Link convention now binding: absolute site paths only, trailing slash, never `.md`, never `/docs/`. Queued at the usage reset: Sonnet survey of the stylebook catalog for rules that would make pages read better, plus a feasibility check on using the design-system package (icons or components) inside the Starlight site for UI-element anchors; then an Opus editorial pass over the wave 1 pages before wave 2 owners start, so later waves inherit the improved style.
- 18:40 PT user diagnosis of wave 1: "reads like a code report rewritten for humans." Fable agrees. Root cause: pages were cut per inventory item and written from the implementation outward (page opens by defining the object; every option enumerated in a table; flat "You are taken to your home page" outcomes; 16 student account pages for what a student experiences as a few moments). Binding correction for the editorial pass and for all later waves: start from the reader's moment and intent ("Your teacher gave you a code"; "It's the first day and you need your class set up"); say why a step matters when it is not obvious; show what the reader will see and what they will probably want next; use the reader's mental model (a teacher's period 2, not a section with a login type); merge pages that are one moment for the reader and split pages that are two; enumerate options only when the reader must choose, and then help them choose. The inventory maps coverage; it must never dictate page boundaries.
- 20:05 PT (session reset): style samples from 19 live pages landed in `.docs-work/raw/style-samples.md`; "Page shape" conventions added to domains.md (imperative task titles, situation-first opening, one primary path, "If you..." variants, inline crops at the step, short Next steps). `docsScreenshot` gained `{locator}` for control crops. Wave 1 recut launched: three docs-opus editors (accounts, classrooms, curriculum) following `.docs-work/recut-brief.md`; each records moves in `domains/<domain>/renames.md` for a cross-domain link patch afterwards. The Sonnet catalog survey of extra stylebooks was superseded by the live-site sampling; not run.
- 20:15 PT: wave 1 recut complete. accounts 24→28 (settings split per outcome, sign-in methods merged), curriculum 15→11 (find-a-course, prepare-to-teach, start-your-course merges), classrooms 23→23 (choose-a-login-type split out as a decision page; assessments and scores merged). No cross-domain links needed patching. Dangling links left for later waves: `/district-administrators/lms-integration/` (integrations owner picks the canonical path), `/teachers/ai/evaluation/` (ai-features). Screenshot count unchanged (7); a dedicated control-crop pass is still owed to the user. Wave 2 in flight: integrations-schools-districts, labs-and-learning-experience; DSCO-in-docs feasibility check running on Sonnet.
- 20:20 PT DSCO-in-docs decision (evidence `.docs-work/raw/dsco-in-docs.md`): `@code-dot-org/component-library` is 0.1.0-alpha.1, never published, and its README says it cannot be installed outside the monorepo; icons are FontAwesome class names that need the external kit; embedding would require `@astrojs/react` + React in `docs/site` and converting pages from `.md` to `.mdx`. Decision: UI anchors are element-crop screenshots via `docsScreenshot(..., {locator})`, inline at the step; bold for UI labels. Revisit when the library is published. A dedicated control-crop pass over wave 1 pages is queued after the wave 2 owners report.
- 20:35 PT: integrations-schools-districts done (6 district-admin pages incl. landing, 3 developer pages, 1 journey, 1 screenshot). Canonical LMS setup page `/district-administrators/integration/connect-your-lms/`; inbound links patched. Deep-linking sentence tightened to the two items the code offers (Music Lab, AI Foundations 2025). Owner's question on the student LMS page ("your school administrator may need to finish setup" though the form has no auth gate): wording kept, an administrator is still the realistic actor. Screenshot pass over wave 1 pages launched (target 20-30 control crops).
- 20:50 PT: projects-and-sharing done (19 pages, 6 journey tests, 4 crops); labs-and-learning-experience done (26 pages, 8 journey tests, 10 screenshots; OQ17 verdict: lab2 keeps the appOptions contract with a thin payload plus async level properties). Three dangling links from labs patched to real pages. Wave 3 in flight: ai-features; professional-learning launching now; screenshot pass over wave 1 still running.
- 20:55 PT user: "needs more visuals." Visual density raised in domains.md (about one image per task page; orientation shots allowed on screen-describing concept pages). Screenshot agent told to aim for 50-60 on wave 1 and to continue into labs, projects, teachers/learning, and district-administrators.
- 21:05 PT user: the tree is flat and hides scope ("some things only apply to certain level types or labs or feature sets"). Convention added: an "Applies to" line under the title on every scoped page, and nested directories with hub index pages where the reader thinks hierarchically (lab-specific pages under `students/labs/<lab>/`). Queued: an IA pass over `docs/students/` and `docs/teachers/` (move, hub, Applies-to, renames file, link patch) at the next free slot, before the developer domains start.
- 21:10 PT user: "organize like topics; some pages are too small, combine and add detail." Convention "Topic pages" added: topic-level pages with task H2s, merge sub-150-word pages, detail per section (result, variant, common failure). This supersedes the settings split from the recut. The queued IA pass now does: topic reorganization, merges with evidence merges, nested lab/feature directories with hubs, Applies-to lines, renames file, cross-domain link patch, rebuild.
- 21:20 PT: ai-features done (7 pages; 6 features GA, 1 per-section, 13 dark and omitted, Chatter not AI; journeys BLOCKED on provider credentials; `/teachers/ai/evaluation/` patched to `/teachers/ai/review-ai-evaluation/`). levelbuilder-and-curriculum-pipeline launched. IA pass waits for the screenshot agent to finish (same files).
- 21:30 PT: screenshot pass done (46 images on 45 pages, 18 new journey tests green; BLOCKED: feedback/rubric/code-review (need submitted work), roster import (OAuth), incubator (external), lock/hide lessons (curriculum data), certificates (completed courses), password reset (reCAPTCHA)). It reported 11 failing tests in earlier specs (labs-learning, projects-and-sharing, assign-course, join-section); the IA pass diagnoses and fixes them. IA pass launched over `docs/students/` and `docs/teachers/` (topics, merges with evidence union, nested lab dirs with hubs, Applies-to, journey path updates, link patch). In flight: professional-learning, levelbuilder, IA pass.
- 21:45 PT: professional-learning done (7 teacher pages incl. hub, 1 principal page, 3 developer pages, 1 journey, 3 screenshots). Verification: AUTHORIZED_TEACHER via 7 paths (2 PD, 5 account-lifecycle). Contradiction under investigation (Sonnet): application and principal-approval views exist but no route answered locally. Curriculum "get verified" links route to `/teachers/professional-learning/become-a-verified-teacher/` via the IA pass. In flight: levelbuilder, IA pass, PD-routing check.
- 21:55 PT: levelbuilder-and-curriculum-pipeline done (hub + 4 pages; levelbuilder mode off locally so journeys BLOCKED; curriculum open questions settled: hoc_legacy = HoC tutorial engine, practice problems ≠ challenges, widget2 = standalone editor, curriculum_proxy retirement AMBIGUOUS). engineering-platform launched (also writes the developers hubs). In flight: IA pass, PD-routing check, engineering-platform.
- 22:05 PT PD application verdict (`.docs-work/raw/pd-application-routing.md`): teacher, facilitator, and principal-approval routes were removed 2025-09-03 (2f2f2700ad3, controllers deleted next day); views orphaned; `pd_application_principal_approval_url` undefined but still referenced by 4 mailer templates (would raise); intake is external at code.org/apply. PL owner resumed to correct `apply-to-a-program.md`, the principal page, and the developer PD pages. Engineering hazard to report to the user: dangling mailer helper.
- 22:10 PT user (class fix): screenshots only where they disambiguate a control; tight crops of that control; full-page and viewport shots are not useful; slice one page into several crops (forgot-password link, OAuth buttons). Supersedes the 20:55 "orientation shot" allowance. Convention rewritten. Class fix queued: sweep every image in all four audience roots after the IA pass finishes (it is moving image files now): replace viewport/full-page images with locator crops or delete them, re-run journeys, update evidence.
- 22:20 PT: PL correction applied (apply-to-a-program deleted and folded into become-a-verified-teacher; principal page reduced to the honest external process; PD object model notes the removed routes and dangling helper). delivery-and-operations launched (operations pages + documentation runbook + `docs:affected` script). In flight: IA pass, engineering-platform, delivery-and-operations. Screenshot class fix waits for the IA pass.
- 22:30 PT: engineering-platform done (7 platform pages, 8 hubs incl. developers root). Build currently broken by an in-flight IA-pass file (`docs/students/labs/internet-simulator.md`, Applies-to line inside frontmatter); verify after the IA pass reports. Session 74%: third slot held open deliberately. Proposed edits to existing docs collected for the final report: TESTING.md (PhantomJS, Sauce Labs), docs/pegasus-dashboard-integration.md (superseded).
- 22:55 PT: IA pass done (students 63→40 content pages + 8 hubs; teachers 41→25 + 6 hubs; 19 merges; 57 renames; Applies-to on 15 pages). It could not run the build (nvm in its shell); Fable built it, fixed `internet-simulator.md` frontmatter, evidence check passes, 139 pages. Screenshot agent resumed with the class fix (crops only, delete non-disambiguating and full-page images, make the journey suite green or honestly fixme'd). In flight: delivery-and-operations, screenshot class fix. Session 77%.
- 23:00 PT user: lab pages are "shallow descriptions of labs"; they must follow the problem-solving frame. Labs owner resumed: each lab becomes a hub with problem-shaped topic pages (make something happen, add a screen, why it doesn't run, save data, ...) grounded in code, Cucumber, and walked journeys. PL page links to studio URLs fixed to absolute; deleted application page link redirected.
- 23:05 PT user: "same applies throughout, the user manual is here to help inform, not be a spec sheet." Convention "Help, not a spec sheet" added as the editorial bar. Queued: independent editorial review of all student, teacher, and district-administrator pages against it, one Opus reviewer-editor per audience, after the screenshot class fix and the labs rewrite land (both edit those pages).
- 23:15 PT: delivery-and-operations done (6 operations pages + hub; runbook: corpus-and-conventions, verification, update-after-deploy, periodic-reconciliation, agent-orchestration; `docs/site/scripts/affected-docs.sh` wired as `npm run docs:affected`). All 11 domains written. Session 84%; in flight: screenshot class fix, labs rewrite. Next window: editorial review per user audience ("help, not a spec sheet"), Fable integration review, final rebuild, summary to the user.
- 23:25 PT: labs rewrite done (13 hubs + 22 problem-shaped topic pages; all STRONGLY_SUPPORTED; lab-level viewport images removed under the crops-only rule; ungrounded topics omitted: App Lab data blocks, Web Lab upload timing, NetSim multi-user, Java Lab theater). Session 87%; in flight: screenshot class fix only.
- 23:35 PT user: labs were split into hub + children but "many can be a single long page." Fable merged each lab's hub and topic pages into one topic page per lab (13 pages, sections as H2, evidence unioned, inbound links to sections rewritten as anchors; `.docs-work/domains/labs-and-learning-experience/renames-merge.md`). Rule clarified for the editorial review: a hub + children split is for a group of distinct topics (labs as a group), never for one topic's sections.
- 23:50 PT user: merged lab pages "do not read coherently; rewrite; only when you get it right apply to other pages." Exemplar process adopted: one Opus editor rewrites `docs/students/labs/app-lab.md` as a coherent guide (situation opening, prose orientation, sections in the order a student's first hour goes, hand-offs between sections, symptom-led troubleshooting, short Next steps). User reviews; only then does the pattern propagate to the other labs and to the audience-wide editorial review. Session 88%.
- 00:05 PT: screenshot class fix done (46 refs on 45 pages → 11 crops on 6 pages, 12 PNGs; 7 specs fixed, 3 simplified, 8 tests fixme'd for lab-load timeouts under parallel workers). Fable set `workers: 1` and `fullyParallel: false` in `playwright.docs.config.ts` (one shared dev server). Follow-up for reconciliation: un-fixme the lab tests now that runs are serial; confirm no VERIFIED page relies on an image whose test is fixme'd. In flight: App Lab exemplar only. Session 89%.
- 00:15 PT: App Lab exemplar agent hit the 90% session guard after gathering evidence and planning; state saved (scratchpad app-lab-rewrite-state.md). Paused until the 1am PT reset; resume the same agent by message, then rebuild and hand the user the link. Pending after approval: propagate to the other 12 labs, then the per-audience editorial review, then Fable's integration review and final summary.
- 00:30 PT user: run a blind persona simulation before calling the IA gold-standard. Design: an Opus planner writes personas (per audience and moment, with goal, prior knowledge, starting state, pass condition) to `.docs-work/persona-study/plan.md`; blind Sonnets (one at a time; shared browser) see only the rendered site at http://127.0.0.1:4321, navigate as the persona, then attempt the task in the local app with a throwaway account following the docs literally, and report clicks-to-page, the answering sentence, where they got lost, whether each screenshot helped, success or failure, and what the page should have said. Opus synthesizes into a ranked IA and page-fix list that drives the editorial pass. Order after the 1am reset: App Lab exemplar resume; persona study (planner + sequential Sonnets); synthesis; then fixes, exemplar propagation, editorial review.
- 01:05 PT (reset; session 7%, week 72%, tier 66%): App Lab exemplar resumed; persona-study lead launched (12-16 blind scenarios, one Sonnet participant at a time using the shared browser against the rendered site and the local app; findings to `.docs-work/persona-study/findings.md`). Cap accounting: exemplar + lead + one participant = 3.
- 01:05 PT: App Lab exemplar written (749 words; first-hour arc; blocks-vs-text folded into "Make it respond"; symptom-led troubleshooting; labels corrected to "Show Text" and "New screen..."). Fable removed the pre-announcing sentence and moved Applies-to under the title. Awaiting user review before propagating to the other 12 labs. Persona study in flight.
- 01:50 PT persona study done (`.docs-work/persona-study/findings.md`, 14 participants: 7 HELPED, 7 PARTLY, 0 FAILED). Navigation fine (1-3 clicks). Page-level failures: label mismatches with the product (class problem: VERIFIED pages were not verified at label level), answers deferred to other pages, missing prerequisites, no troubleshooting for common failures ("Invalid section code"), controls named without a crop where readers got lost, CodeAI/Code.org confusion for young students. Decisions: no `keywords` frontmatter field; put the reader's words (error text, synonyms) in the prose. Journeys must assert every bold label. Fix wave launched, split by audience (students excl. labs, teachers, district-admin + developers). Labs propagation still awaits the user's exemplar verdict.
- 02:00 PT: admin/dev fix wave done (LMS page: no-account-needed and Canvas deep-linking stated where the reader lands; four real LMS options; all form labels asserted by a green journey; Drone flake-vs-break triage; local prerequisites). Developer editorial review launched (independent reviewer-editor, gitlab-docs rubric, reconciles cross-page contradictions). In flight: students fix, teachers fix, developer editorial review. Labs propagation awaits the user's exemplar verdict.
- 02:15 PT: students fix wave done (3 labels corrected and asserted; sharing answer relocated; 3 troubleshooting entries; parent page lists the six data categories; naming bridge sentence). Student editorial review launched (labs excluded). In flight: teachers fix, developer editorial, student editorial.
- 02:40 PT: teachers fix wave done (9 labels corrected and asserted; 3 crops from green journeys; AI pages name CSD/CSP; M5 product inconsistency recorded in evidence). Developer editorial done (Honeybadger+Sentry reconciled; AMBIGUOUS added to evidence schema; 16 self-referential openings removed). Teacher and district-admin editorial reviews launched; student editorial in flight. Weekly usage 76%.
- 03:05 PT: student editorial done (34 pages; corrected pair-programming location, "Email or Username", "Show Text", "Leave section", "Report Abuse"; removed raw URL patterns; product notes: pair programming only via section sign-in checkbox, no student-side color legend). District-admin editorial done (LMS pages merged; registration-form image removed as non-disambiguating; cost answer inline for principals). Journey reconciliation running (Sonnet). In flight: teacher editorial, journeys. Next: Fable integration review, rebuild, summary.
- 03:20 PT: teacher editorial done (22/38 pages edited; 4 more label fixes; implementation names removed; notifications inventory deleted; 3 orphan images removed). Fable integration checks: 140 pages, frontmatter clean, 0 dangling links, 13 images all present, no implementation-name leaks in user pages, one deliberate "Code.org" bridge sentence; 10 pages under 150 words kept (single consequential actions). Journey reconciliation in flight; then final summary. Labs propagation awaits the user's verdict on the App Lab exemplar.
- 03:35 PT: journey reconciliation done (43 tests: 31 pass, 12 skipped with reasons, 0 fail, stable over 3 serial runs; 3 fixmes replaced by real tests; latent bug fixed where `test.fixme(title, reason)` registered zero tests; `working-on-a-level` downgraded to STRONGLY_SUPPORTED). All agents finished. Summary delivered to the user. Open: user's verdict on the App Lab exemplar gates the other 12 lab pages; commit/PR and stylebook vendoring are the user's decisions.
- 03:45 PT user rejected the App Lab exemplar: "the framing is off. it is documentation not a story. apply the styleguide. this is bad." Root cause: Fable's "Reader first" convention (situation openings, section hand-offs) over-corrected the "code report" critique into narrative. Convention replaced by "Documentation, not a story": GitHub Docs structure applied literally (goal in title and one-sentence intro; About only as needed; Prerequisites; imperative numbered procedures with location before control; one-line verification; alternatives separate; Troubleshooting led by the problem; Further reading). Exemplar being rewritten under that rule; nothing propagates until the user approves.
- 03:55 PT user on the stylebook-shaped exemplar: "better but prereq isn't necessary. making a button respond is more like an answer key. it should read like a user manual for App Lab, not an answer key for students." Convention "User manual, not an answer key" added. Exemplar being reshaped around operating the tool (design surface, code surface, running and debugging, blocks/text), with no curriculum recipes and no Prerequisites section.
- 04:10 PT user on the tool-manual exemplar: "better but it feels like it's describing really simple operations. do gold-standard user manuals really go through simple operations?" Sonnet sampling launched: how VS Code, GitHub (editor/Codespaces), Microsoft app help, Google Docs/Classroom, MDN DevTools, Apple Pages, Scratch, Replit, and Code.org's own support center treat simple operations, what earns a section, typical depth, image use → `.docs-work/raw/tool-manual-samples.md`. Exemplar revision waits on that evidence.
- 04:25 PT tool-manual sampling (13 pages): trivial clicks never earn a heading; headings name UI regions/features; single actions are prose; numbered steps only for multi-step workflows; every section adds options, limits, shortcuts, state, or concept; pages open with orientation; reference pages link to task pages. Convention "Tool reference pages" added. Open question for the user: reference pages in the sample use one full-UI orientation image plus crops; the user's crops-only rule currently forbids it. Exemplar being rewritten as a workspace reference with depth from code and tests.
- 04:35 PT user: "let's try it" on the one orientation image for tool reference pages. After the App Lab reference rewrite lands, the same agent adds one viewport workspace image via `labs-learning.spec.ts`; user judges.
- 04:55 PT: App Lab reference page written (~1,270 words, `type: reference`, regions: Design tab [element tray, properties panel, screens, preview], Code tab [toolbox, blocks and text], running and debugging, troubleshooting; 13 source files added to evidence) plus one viewport orientation image from `labs-learning.spec.ts`. Awaiting the user's verdict on both the shape and the image before propagating to the other 12 labs and running the stylebook-literal pass across all user pages.
- 05:05 PT user approved the App Lab reference page ("looks a lot better") and asked for more images as on VS Code. Convention updated: tool reference pages carry one orientation image plus a crop per region/feature section (6-12). Launching: App Lab crops (exemplar agent), the other 12 lab pages rewritten to the approved shape (labs owner, sequentially, with depth from code), and the stylebook-literal pass on teacher pages; student and district passes follow when slots free.
- 05:15 PT usage checkpoint: session 70% (reset 6am), week 80% (reset Sep 14 9am), tier 68%. In flight: App Lab crops, 12-lab reference rewrite (new spec `labs-reference.spec.ts`), teachers final-form pass. Queued: students and district final-form passes. Weekly guard risk noted to the user; priority order labs > teachers > students > district.
- 05:25 PT: App Lab reference page complete with orientation image + 8 region crops (9 images). Students final-form pass launched (labs excluded). In flight: 12-lab rewrite, teachers final-form, students final-form. Queued: district final-form.
- 05:30 PT user: "apply to rest." Final-form (stylebook-literal) passes: labs, teachers, students in flight; district-administrators and a lighter developers check queued. Then final integration checks, rebuild, summary.
- 05:45 PT: 12 lab reference pages done (grounded in each lab's toolbox/config sources; 8 orientation images via `labs-reference.spec.ts`; Python Lab, Minecraft, Play Lab, NetSim text-only; Java Lab run BLOCKED on javabuilder). Region crops for the 12 labs deferred to last (weekly 81%). District final-form pass launched. In flight: teachers, students, district.
- 06:00 PT: teachers final-form done (38 pages; 5 converted to reference: your-home-page, manage-your-settings, managing-your-roster, tracking-progress, manage-workshops; choose-a-login-type → concept decision page). Reference-page images for teacher screens not added; deferred to the images pass with the 12-lab region crops. Developers check launched. In flight: students, district, developers.
- 06:20 PT (04:20 PDT clock): students final-form done (34 pages; 3 → reference; curriculum-advice passages removed). District final-form done (6 pages). Developers check in flight. Images pass (12-lab region crops; orientation+crops for student and teacher reference pages) waits for the 6am session reset; session 84%, week 81%.
- 06:25 PT: developers final-form check done (10 pages; scene-setting openings removed). All final-form passes complete. Corpus checks: 140 pages (31 reference / 63 concept / 46 task), frontmatter clean, 0 dangling links, 30 images present, no implementation leaks, no narrative openers in user pages; build + evidence green. Remaining: images pass after the 6am reset (12-lab region crops; orientation + region crops for student and teacher reference pages), then final summary. Week 82%.
- 06:35 PT (reset; session 10%, week 83%, tier 71%): images pass launched as three agents on disjoint files: lab region crops (`labs-reference.spec.ts`), teacher reference pages (home page, settings, roster, tracking progress, manage workshops), student reference pages (settings, project list, your progress). Then final rebuild and summary.
- 07:00 PT: student reference images done (11 images across settings, project list, your progress; feedback/code-review crops BLOCKED on seed data). In flight: labs crops, teacher reference images. Week 84%.
- 07:20 PT: teacher reference images done (10 images across home page, settings, roster, tracking progress, workshop dashboard; PL role tabs skipped: need facilitator/organizer roles). In flight: lab crops only. Week 84%.
- 07:50 PT: lab region crops done (26 crops across 8 labs; 4 labs text-only: no stable seeded level). Images pass complete. Final rebuild and corpus checks green. Project delivered; summary to the user. Week 85%.
- 08:10 PT user: screenshots not useful (progress orientation shows an empty roster; the "progress grid" crop shows the sidebar). Convention "Image truth" added: image matches its section and alt text, realistic seeded state, reviewed by eye before shipping. Image audit launched: two Opus auditors (labs; everything else) open every PNG, classify, and retake with seeded data, re-crop, or delete.
- 08:20 PT user: "giving feedback also lacks screenshots, so the class is you under-applied screenshots." Two classes acknowledged: wrong/empty images (audit in flight) and missing images on task pages where steps name controls. Convention "Coverage" added (crop every named control in realistic state; seed data instead of declaring BLOCKED). Teacher auditor's brief widened to add the missing crops with the seeded section, students, and submitted work.
- 09:00 PT: students/district image audit done (21 images: 13 MATCH, 4 EMPTY retaken/fixed, 2 WRONG REGION fixed, 1 orphan re-wired; progress circles BLOCKED after 4 seeding attempts). Week 87%: no further agents this week after the two in flight (teachers, labs) finish or hit the guard; remaining fixes resume after the Sep 14 reset.
- 09:30 PT: teacher image audit done (17 images: 4 MATCH, 6 EMPTY retaken with seeded section of 4 students + csd-2024 assignment, 3 WRONG REGION fixed, 1 NOT USEFUL retaken wider, 3 orphans deleted; 3 missing crops added). Still open: giving-feedback-and-grading images need a student submission (a Free Response level as the student would do it); AI pages need a provider; roster import needs OAuth. Week 87%; feedback images held for the user's decision (spend now vs after Sep 14 reset).
- 09:45 PT: labs image audit done (43 → 33 MATCH; 5 retaken with Run/blocks state). Fable removed the 7 images the auditor left as EMPTY or uncapturable (App Lab and Game Lab blank workspaces, Java Lab console, Music Lab timeline, Artist canvas, Sprite Lab play area, Web Lab iframe) plus 3 orphan PL PNGs regenerated by its spec (the spec should stop writing them: follow-up). Final state: 141 pages, 73 image refs on 28 pages, 0 dangling links, 0 orphans, build + evidence green. Week 87%. Open: feedback-page screenshots (need a student submission), AI pages (provider), roster import (OAuth); PL spec still writes unused PNGs.
- 10:00 PT user: migrate the docs site from Starlight to Docusaurus with light Code.org branding via the design system; Docusaurus defaults, anti-bespoke, no swizzling without strong reason; verify in a real browser; usage guard raised to 95% for this task. Launched as one docs-opus implementer.
- 10:45 PT: Starlight → Docusaurus migration done. Site at `frontend/apps/docs/` (Docusaurus 3.10.2, workspace app on Node 20/yarn 4), content unchanged at `docs/` (`path: ../../../docs`, include-list of the four roots + index.md, `routeBasePath: '/'`, `markdown.format: 'md'`), four autogenerated sidebars, navbar `doc` items to each audience hub (Fable switched from docSidebar), local search plugin, dark mode switch disabled, CSS-only branding (Infima vars from design-system primitive colors; Geist + Space Grotesk via @fontsource; storybook logo). No swizzles, no Root.tsx, no MUI (no DSCO component used). `docs/site/` deleted; scripts moved to `frontend/apps/docs/scripts/`. Served on :4321 (0.0.0.0). Orphan `district-administrators/index.json` removed.
- 11:00 PT user: image backfill now, Opus, guard 95. Launched one docs-opus agent with priorities: feedback/grading (seed a real student submission via Free Response + Multi levels), student progress, sign-in variants (picture/word sections), teacher task pages without images, lab workspaces with placed blocks, then anything else lacking. Rebuild Docusaurus after each block.
- 11:20 PT user: teacher home page screenshots include the rebrand announcement overlay; "either disable or wait for it." Routed to the backfill agent as priority 1b: dismiss the overlay as a teacher would (or disable via the dcdo mock if a flag governs it), add the dismissal to shared setup, retake affected teacher captures.
- 12:00 PT: image backfill priorities 1-4 done (feedback view with a real Free Response submission, text responses, assessments, progress views filled, picture/word sign-in captures, forgot-password crop, section menu with Archive; rebrand banner + logo transition dismissed in `docsScreenshot`). Priorities 5-6 (lab workspaces with code, misc) BLOCKED: level pages crash with `Aws::STS::Errors::ExpiredTokenException` on the dev server (environment: expired AWS credentials; needs the user to refresh and restart, which agents may not do). Feedback comment box and assessment result tables also blocked by the same error. Week 90% (guard 95).
- 12:30 PT user: "retrigger and I will auth it." `bin/aws_access` (aws-google credential_process, profile cdo-readwrite) refreshed without interaction; level pages 200; last ExpiredToken log line predates the refresh. Backfill agent resumed for the AWS-blocked items, priorities 5 and 6; instructed to stop and report if the error recurs (then the user restarts the dashboard).
- 13:15 PT: backfill round 2 done (feedback comment box + Save and share on a Web Lab level, Game Lab workspace with code, Artist canvas via clip). Fable removed the two remaining empty-state images (App Lab Design workspace, Music Lab timeline). Still open: assessment results table (needs a LevelGroup submission), print certificates (no direct route), App Lab workspace with elements (drag-and-drop), Music Lab timeline, Java Lab console, Web Lab iframe. Week 91% (guard 95): spend paused; resume after the Sep 14 reset unless the user directs otherwise.
- 13:40 PT user: collapse students + teachers (+ district admins) into one generic "User guide" organized by topic; developers stay separate; no persona labels; delegated whole restructure to one docs-opus agent (guard 95; week ~91). Target: `docs/guide/{getting-started,sections,curriculum,labs,projects,progress,ai,professional-learning,integrations,privacy}` + `docs/developers/`; merges of duplicated student/teacher pages; two sidebars; two navbar items.
- 14:10 PT: IA collapse done. Tree: `docs/guide/` (User guide, 10 topic groups, 70 content pages + 11 hubs; 6 merges absorbing 12 pages) + `docs/developers/` + `docs/index.md`; 129 pages total; 82 images; 0 dangling links; two sidebars, two navbar items. Fable removed 11 orphan PNGs (spec-regenerated or leftover). Follow-up: specs that still write unused PNGs (PL landing/catalog, settings account-info) should drop those calls at the next journey pass. Week 92% (guard 95).
- 14:30 PT: PL spec trimmed to the one capture a page uses. Follow-up for the next journey pass: specs still write images that no page references after the image-truth removals and the merges (labs-reference: artist-canvas, game-lab-canvas/workspace, java-lab-console, sprite-lab-play-area, web-lab-workspace; teacher specs: manage-your-settings-* for the merged account-settings page). Drop those calls or repoint them; until then they reappear as orphans on each run. Week ~92%.
- 14:45 PT user: "IA is too mixed (change site language under Labs); fix as a class." Cause: pages placed by origin in the collapse. Placement audit launched (docs-opus): every guide page gets a reader-test verdict (KEEP/MOVE/SPLIT), misfits move with images, evidence, links, spec paths, and hubs updated; groups checked for one-subject coherence.
- 15:15 PT: placement audit done (59 pages: 55 KEEP, 4 MOVE: change-the-site-language → getting-started, answering-and-submitting → progress, hide-units-and-lessons → curriculum, approve-a-teacher-application → professional-learning). Fable nested the three generic level pages under `labs/working-in-a-level/` (subcategory, position 1) so Labs lists them first, then the 13 labs; renamed lab titles from "<Lab> workspace" to "<Lab>" per the user. Build + evidence green.
