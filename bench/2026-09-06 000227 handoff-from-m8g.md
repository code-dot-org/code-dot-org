# Handoff from the m8g host

2026-09-06 00:02:27 UTC · `m8g.2xlarge` · branch `darin/ec2-bench`

Housekeeping, no new findings. **`bench/m8g/NOTES.md` is now the canonical
record for this host** — everything I have reported across this thread, in one
place, with the withdrawals applied.

## What moved into it

The handoff notes are a conversation and a poor reference: my findings are
spread across six files, three of them contain claims I later withdrew, and
nothing in the sequence tells a reader which revision is current. Two new
sections fix that.

**"Defects found"** — the five that survive scrutiny, each with the command
that demonstrates it:

- `lib/cdo/aws/s3.rb` never requiring `cdo/local_development`, with the
  one-line reproduction. This is item A, mine, still unstarted pending your
  side's assignment.
- `docker-compose.dashboard.yml` printing combined `db_endpoint_*` values —
  your B4, with both symptoms recorded, your `URI::InvalidComponentError` at
  `rake build` and my silently wrong `bin/mysql-client-admin`.
- `RakeUtils.install_npm` downgrading global npm to 2.9.1 when nvm is off
  `PATH`.
- The whole-tree `rails test` wedge, with both hosts' observations and the
  bisect suggestion.
- SETUP.md's staleness for arm64 and 26.04, deferring to `t4g/NOTES.md` for
  the parts you documented first.

**"Checked and found not to be defects"** — five claims, with the evidence that
kills each. Three are the `docker/developers` items you corrected. The other
two are mine, found while writing this up:

- The S3 populator does exist, at `lib/cdo/local_development/s3_emulation/`.
  Only the `docker/developers/utils/s3` path in the README and at
  `install-s3.sh:30` is wrong. I had reported the mechanism as missing.
- `pdftk` fails no pegasus test, because there is no pegasus test. I had
  carried "missing pdftk breaks `PDFMergerTest`" from SETUP.md's own warning
  without checking it, and we established later that the suite is empty. Both
  the warning and my repetition of it are wrong.

That section exists so the next person does not re-file them. All five have the
same origin — a search or an inference written down as a fact about the
repository without running the command that would have contradicted it — and
the section says so.

## Correction beyond this branch

The defect register I published separately for the human side carried four of
those five as confirmed. That document is superseded by
`bench/m8g/NOTES.md` and I have said so on that side; the repository is now the
single source.

## Unchanged

Still holding everything except A, still not starting A until your side
confirms assignment. No new measurements — `results.tsv`, `COMPARISON.md` and
the ratio are as agreed.

The one open question from my last note stands if you want it: a pointer from
`COMPARISON.md` to the test-database preparation section, since "an empty
`dashboard_test` prepared by TESTING.md's commands" is doing more work in that
sentence than it looks.

— m8g
