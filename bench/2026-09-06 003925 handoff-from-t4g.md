# Handoff from the t4g host

2026-09-06 00:39:25 UTC · `t4g.2xlarge` · branch `darin/ec2-bench`

Assignment confirmed. **I am starting items 1, 2 and 3. A is yours — go.**

Your B1/B2/B3 withdrawals and the two you found yourself are all verified here:
`lib/cdo/local_development/s3_emulation/populator.rb` exists, so only the
`docker/developers/utils/s3` reference is stale — and it is a comment at
`install-s3.sh:30`, not code. The `PDFMergerTest` warning is still live at
`SETUP.md:226`; it is in scope for item 1 and I will remove it there.

`COMPARISON.md` now says why the empty test database matters, with a pointer to
your preparation section. Your framing was right: the sentence was carrying
more than it looked like.

## What I am taking

**1. SETUP.md, arm64 and current-distro.** Documentation only. The apt mirror,
`libyaml-dev`, the gcc-13 Ruby, arm64 Chrome, `pdftk` and `chromium-browser`
having no arm64 candidate, the stale version table, and removing the
`PDFMergerTest` warning for a suite that does not exist.

**2. `ImageLibTest`.** `dashboard/test/lib/image_lib_test.rb:98` compares
`compare -metric ae` output against the string `'0'`; ImageMagick 7 prints
`0 (0)`. Fixing the test, not the library — the library is behaving.

**3. B4 and the README line.** `docker-compose.dashboard.yml` emitting combined
`db_endpoint_*` values, and `docker/developers/README.md` not saying which
directory to run from.

## How

One commit per item on this branch, each standing alone so they can be split
into separate PRs if the human side wants that. Item 2 I will verify by running
`dashboard/test/lib` and showing the three tests green; items 1 and 3 are
documentation and configuration, and I will state plainly which parts I have
executed and which I have only read.

Scope is minimal and Graviton- or EC2-facing. I am not touching `image_optim`,
the `config/blocks` write churn, or the `dashboard/test/testing` isolation
failure. If an item grows past a few lines I will stop and say so rather than
widen it.

## Still not being done by either of us

`install_npm` — raised, not patched, needs a maintainer's view on intent.
The wedge — issue only. Neither is started.

— t4g
