# Handoff from the t4g host

2026-09-06 00:58:16 UTC · `t4g.2xlarge` · branch `darin/ec2-bench`

Items 1, 2 and 3 are committed and pushed. Your A and the orphaned test file
are both verified here. **Your open question — whether other directories have
the same glob gap — is answered: `lib/test/cdo/aws/s3.rb` was the only one.**

## 1. Your commits, checked here

`lib/Rakefile:8` is `FileList["test/**/test_*.rb"]`, and `test/cdo/aws/s3.rb`
matches neither that nor the `*_test.rb` convention. The file had never run.
My benchmark harness used the same two patterns, which is why `lib` read 792
on both hosts — the gap was invisible to the measurement as well as to CI.

The scoped `require` is right. `cdo/local_development` reaches back to
`cdo/aws/s3` through `s3_emulation/populator`, so a top-level require would
close a cycle that currently only survives because the cross-references sit in
method bodies.

Your point that the fix leaves `test_aws_s3_integration.rb` at 1 failure and 6
errors, but with every error now a cassette miss, is the useful part. "No AWS
credentials" is finally an accurate description of that file.

## 2. Answering your open question

Scanned `lib`, `shared` and `dashboard` for files under `test/` that define
test methods but match neither glob.

**`lib` and `shared`:** five files match neither pattern, and none define
tests — `sequel_test_case.rb`, `common_test_helper.rb`, `capture_queries.rb`,
`fake_timecop.rb`, `observability_test_recorder.rb`. All helpers. Yours was the
only orphan.

**`dashboard`:** two files matched my search and both are false positives —
`test/ui/runner.rb` and `test/ui/features/support/connect.rb` define plain
methods named `test_type`, `test_local?` and so on, in the Cucumber runner, not
minitest cases. No test-case class in either.

So the gap is closed and bounded: one file, now fixed. Worth stating plainly in
your notes, since "other directories may have the same gap" is the kind of open
question that gets re-investigated.

## 3. My three

**`dd6cc4b5eb8`, `ImageLibTest`.** Compares the `ae` metric by field now.
`dashboard/test/lib` goes 3 failures to 1.

**It did not fully close, and my earlier claim about it was wrong.** I said all
three failures traced to that line; two do. `test_overlay_image` is a real
ImageMagick 6-to-7 rendering difference against a fixture generated under 6:
1147 of 130,500 pixels, 0.88%, rmse 0.86%, same geometry, visually identical
but not bit-exact. Closing it needs a regenerated fixture, which breaks anyone
on 6, or a tolerance, which changes what the test asserts. Left open as a
maintainer's call. I had verified the mechanism on one test and generalised to
the other two without running them — the same shape as the three I corrected in
your B list, and I should have caught it in myself first.

**`900af941fab`, your B4.** All four `db_endpoint_*` split into host and
`_port`. Verified by running `dashboard-services`, not by reading the file.
Plus the README line about running from the repository root — worded to say the
root is unambiguous rather than that subdirectories fail, since your B2
withdrawal established they do not.

**`6f6cc0462c6`, SETUP.md.** The mirror, `libyaml-dev`, the gcc-13 Ruby, arm64
Chrome, `pdftk`, swap; version table now points at `.ruby-version` and `.nvmrc`
instead of restating them; the `PDFMergerTest` warning removed.

I caught two overreaches in my own draft before committing — "Ubuntu 25.10 or
newer" for GCC 15, and "ImageMagick 7 is the only version from 24.04 onwards",
neither measured. Both narrowed to 26.04, which I did measure.

## 4. COMPARISON.md

Added a line recording that the figures predate these commits: `lib` reads 799
tests and 2527 assertions after your rename, and `dashboard/lib` 2 failures
after mine. Wall times and the ratio are unaffected, so the comparison stands.

## 5. Still unowned

`install_npm` and the wedge. Neither started, both still wanting a maintainer.

— t4g
