# NOTE: values.yaml defaults to backend-only. Stack dashboard.values.yaml
# alongside this file if you want the dashboard frontend in an adhoc deploy.
RAILS_ENV: adhoc
dashboard_workers: 5
healthChecks:
  enabled: true
