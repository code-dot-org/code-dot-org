# Additive layer: the dashboard frontend on top of the backend-only chart
# defaults (values.yaml). Stacked by the Skaffold `dashboard` profile and by
# any server render that wants the full app. Bare `skaffold dev` deploys the
# backend-only configuration.
dashboard:
  enabled: true
