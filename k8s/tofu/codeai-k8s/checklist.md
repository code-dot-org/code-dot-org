- [x] Create `k8s/tofu/codeai-k8s/` scaffolding: `README.md`, `plan.md`, `checklist.md`, `implementation-notes.md`, `cluster/`, `cluster-infra/`, `cluster-infra-argocd/`, and the split ESO modules
- [x] Copy/refactor `codeai-k8s-pre-reorg/eks-cluster` into `codeai-k8s/cluster`
- [x] Remove Route53, ACM, ExternalDNS, ESO Helm, and Gateway API CRDs from `cluster`
- [x] Rework `cluster` outputs so later roots consume remote state instead of duplicate cluster-shape tfvars
- [x] Build `codeai-k8s/cluster-infra` as the AWS-side root
- [x] Move Route53 zone and ACM certificate resources into `cluster-infra`
- [x] Keep Dex Google client secret bootstrap in `cluster-infra`
- [x] Split ESO into `cluster-infra/infra/standard-envtypes/modules/eso-per-envtype` and `cluster-infra-argocd/infra/eso-per-envtype`
- [x] Move ESO IAM role creation into `cluster-infra`
- [x] Move Kargo SecretStore IAM into `cluster-infra`
- [x] Keep Kargo GitHub webhook secret bootstrap in `cluster-infra`
- [x] Refactor `external-dns` in `cluster-infra` to IRSA-only wrapper mode with `observability_tag = null`
- [x] Refactor `aws-load-balancer-controller` in `cluster-infra` to IRSA-only wrapper mode
- [x] Export all `cluster-infra` outputs required by later phases
- [x] Create the interim `cluster-infra` non-AWS bootstrap module
- [x] Move ESO Helm into the `cluster-infra` non-AWS bootstrap module
- [x] Move Kargo git credentials into the `cluster-infra` non-AWS bootstrap module
- [x] Move the Kargo GitHub organization webhook into the `cluster-infra` non-AWS bootstrap module
- [x] Keep `cluster-infra-argocd` as the remaining K8S-only add-on root
- [x] Move Gateway API CRDs into `cluster-infra-argocd`
- [x] Install ArgoCD in `cluster-infra-argocd`
- [x] Move app-of-apps bootstrap into `cluster-infra-argocd`
- [x] Install Dex in `cluster-infra-argocd`
- [x] Make Dex chart-owned in `cluster-infra-argocd`, with Google secrets pulled through ESO
- [x] Create ESO Kubernetes resources in `cluster-infra-argocd`
- [x] Create frontend pod security group policy in `cluster-infra-argocd`
- [x] Create AWS ALB gateway class resources in `cluster-infra-argocd`
- [x] Create the remaining Kargo SecretStores in `cluster-infra-argocd`
- [x] Create Kargo GitHub webhook `ExternalSecret` in `cluster-infra-argocd`
- [x] Refactor `external-dns` in `cluster-infra-argocd` to a plain `helm_release` using the role ARN from `cluster-infra`
- [x] Refactor `aws-load-balancer-controller` in `cluster-infra-argocd` to a plain `helm_release` using the role ARN from `cluster-infra`
- [x] Write all split mismatches to `implementation-notes.md`
- [x] Run `tofu fmt -check` under `k8s/tofu/codeai-k8s`
- [x] Run `tofu init -backend=false && tofu validate` in `cluster`
- [x] Run `tofu init -backend=false && tofu validate` in `cluster-infra`
- [x] Run `tofu init -backend=false && tofu validate` in `cluster-infra-argocd`
- [x] Confirm `k8s/tofu/codeai-k8s-pre-reorg` remains unchanged
- [x] Code review phase: can we make the code cleaner and simpler? if yes, repeat until as simple as possible without losing anything
- [x] Match previous tofu phase: is this a perfect replica with existing tofu files in `codeai-k8s` folder modules? if not, improve and iterate
