apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - ../../base

namePrefix: prod-

patchesStrategicMerge:
  - production-patch.yaml

configMapGenerator:
  - name: production-config
    literals:
      - RAILS_ENV=production

# Additional configurations specific to the production environment
