apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - ../../base

namePrefix: dev-

patchesStrategicMerge:
  - development-patch.yaml

configMapGenerator:
  - name: development-config
    literals:
      - RAILS_ENV=development

# Additional configurations specific to the development environment
