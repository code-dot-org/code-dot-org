# Backend-only deploy target for Skaffold. The root docker family does not yet
# define a dashboard image containing the frontend assets.
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - ../backend
patches:
  - path: active-job-worker-root.patch.yaml
