apiVersion: kustomize.config.k8s.io/v1alpha1
kind: Component
resources:
  - service.yaml
  - statefulset.yaml
patches:
  - path: locals.yml.patch.yaml
secretGenerator:
  - name: cdo-local-secrets
    behavior: merge
    type: Opaque
    envs:
      - cdo-local-secret-mysql.secret.env
