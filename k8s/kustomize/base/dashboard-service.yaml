apiVersion: v1
kind: Service
metadata:
  name: cdo-dashboard
  labels:
    app.kubernetes.io/component: dashboard
spec:
  selector:
    app.kubernetes.io/component: dashboard
  ports:
    - name: http
      port: 3000
    - name: http-hmr
      port: 9000
