# The full application: shared backend configuration plus the dashboard
# frontend. Server environment overlays compose this base.
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
components:
  - ../components/backend
  - ../components/dashboard
