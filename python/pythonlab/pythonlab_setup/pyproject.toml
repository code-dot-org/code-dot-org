[project]
name = 'pythonlab_setup'
version = '0.1.0'
requires-python = '>=3.12'

dependencies = [
  'matplotlib >=3',
  'requests >=2',
]

[tool.pytest.ini_options]
testpaths = ['tests']

[tool.uv.scripts]
test = 'pytest'

[build-system]
requires = ['hatchling']
build-backend = 'hatchling.build'
