# Coordinates are percentages of the screen's width and height, so a layout
# looks the same whatever size the preview panel is.
MIN_COORDINATE = 0
MAX_COORDINATE = 100

# Element kinds, as they appear in a published scene.
BUTTON = "button"
LABEL = "label"
