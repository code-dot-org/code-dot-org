/** @file Build configuration constants that can be included by all of our
 *       code-studio build scripts. */
'use strict';

/** @type {string} Path to JS+JSX source files, relative to code-studio root. */
exports.JS_SRC_PATH = 'src/js/';

/** @type {string} Path to JS+JSX build output, relative to code-studio root. */
exports.JS_BUILD_PATH = 'build/js/';
