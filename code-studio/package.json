{
  "name": "@cdo/code-studio",
  "version": "0.0.1",
  "description": "Pipeline for static assets used by our Code Studio rails app (i.e. dashboard)",
  "author": "Code.org",
  "license": "Apache-2.0",
  "engines": {
    "node": ">=0.10.25 <0.13",
    "npm": "^2.9.1"
  },
  "scripts": {
    "build": "browserify --debug src/js/blockly_editor.js -o built/js/blockly_editor.js",
    "build:min": "browserify src/js/blockly_editor.js | uglifyjs -o built/js/blockly_editor.js",
    "prebuild": "mkdir -p built/js",
    "prebuild:min": "mkdir -p built/js",
    "clean": "rm -rf built",
    "env": "env",
    "preinstall": "rm -f node_modules/vendor",
    "postinstall": "ln -s ../vendor node_modules/vendor",
    "lint": "npm run lint:js -s",
    "lint:js": "jshint src/js/*.js",
    "start": "echo \"Unimplemented.\"",
    "stop": "echo \"Unimplemented.\"",
    "test": "test"
  },
  "devDependencies": {
    "browserify": "^12.0.1",
    "jshint": "^2.8.0",
    "uglify-js": "^2.6.1"
  },
  "dependencies": {
    "@cdo/vendor": "file:../vendor",
    "codemirror": "5.5",
    "marked": "0.3.2"
  }
}
