## i18n / String Rules

- Do NOT add new i18n keys or use translation functions (`locale.t()`, `i18n.t`, etc.).
- Do NOT modify existing i18n usage.
- All new strings must be plain English strings in code.

This overrides default best practices. Follow this even if other parts of the codebase use i18n.
