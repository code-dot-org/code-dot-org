%h2 Users
%table
  %tr
    %th
    %th Name
    %th Email
    %th Phone
  -database[:users].all.each do |row|
    -profile = JSON.parse(row[:google_oauth2])['info']
    -image = profile['image'] || '/images/user_default_image.jpg'
    %tr
      %td
        %img{src:image}
      %td
        %a{href:"/users/#{profile['email']}"}= profile['name']
      %td
        %a{href:"mailto:#{profile['email']}"}= profile['email']
      %td
      