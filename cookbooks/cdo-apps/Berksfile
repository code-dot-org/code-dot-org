source 'https://supermarket.chef.io'

metadata
%w(
  repository
  github-access
  secrets
  postfix
  varnish
  mysql
  ruby
).map{|x|"cdo-#{x}"}
  .push('sudo-user')
  .each do |dep|
  cookbook dep, path: "../#{dep}"
end
