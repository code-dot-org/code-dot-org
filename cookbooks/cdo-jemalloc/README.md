cdo-jemalloc Cookbook
=====================================

Compiles and installs [`jemalloc`](http://jemalloc.net/), a general purpose `malloc(3)` implementation that emphasizes fragmentation avoidance and scalable concurrency support.
