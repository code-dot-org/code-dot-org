// Adds a .fa-fw class to all icon elements
// Used to maintain consistent spacing when
// icons have varying widths and heights
document.querySelectorAll("i").forEach((icon) => icon.classList.add("fa-fw"));
