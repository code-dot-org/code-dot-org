%h2 The Frozen tutorial is supported in the following languages
%ul{:style=>"background: #0094ca padding: 20px;"}
  - Languages.get_frozen_languages.each do |properties|
    -if properties[:frozen_b] == true
      %li.bullet= properties[:crowdin_name_s]
