
= view :header

.container

  - if header['nav']
    .row
      .col-sm-3{style: "margin-top:50px; background-color: rgba(0,0,0,0.02); font-size: 16px; padding-left: 0px; padding-right: 0px;"}
        = view(header['nav'])
      .col-sm-9
        = body
  - else
    = body

%br/
%br/
%br/

= view :footer
