#whats_next
  %h2{:style=>'margin-top:25px'}=hoc_s(:whats_next_header)
  %ul{:style=>"line-height:28px"}
    %li=CGI.unescapeHTML(hoc_s(:whats_next_how_to)).gsub('/how-to', resolve_url('/how-to'))
    %li=CGI.unescapeHTML(hoc_s(:whats_next_how_to_promote_link)).gsub('/promote', resolve_url('/promote'))
    %li=CGI.unescapeHTML(hoc_s(:whats_next_try)).gsub('%{learn_url}', resolve_url('/learn'))
