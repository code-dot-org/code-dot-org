// This file should contain additional helper functions.

// alert('hello world');


(function() {
    // BS is the namespace we'll be putting in the bootstrap support functions.
    window.BS = {};
    
    BS.nonemptyConstraint = function(x) {
        return !(x === "");
    };

    

}());