export type School = {
  nces_id: string;
  name: string;
  zip?: string;
  city?: string;
  state?: string;
  latitude?: string;
  longitude?: string;
  school_type?: string;
  isNew?: boolean;
  teachesCs?: string;
};
