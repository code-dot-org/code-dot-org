# Cached units

These are all normalized `UnitData` files which represent all unit and level
information for a course/unit/script.

See `src/app/models/unit.ts` for a type description for these files and the
`loadUnit` function to retrieve and parse them.
