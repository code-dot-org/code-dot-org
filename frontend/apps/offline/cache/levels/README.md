# Cached levels

These are all normalized `LevelData` files in JSON form which represent level
data from the `.multi`, `.bubble`, and `.level` files for our curricula.

See `src/app/models/level.ts` for a type description for these files and the
`loadLevel` function to retrieve and parse them.
