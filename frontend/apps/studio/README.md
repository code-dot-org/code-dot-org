# Code Studio (Experimental)

Studio is the experimental host React application for Code.org's frontend — curriculum labs, teacher tools, and other Code.org features. It is served by Rails at `/app` and is not a library package; it has no exports and is never imported by other packages. The current frontend is called `apps` and is located in the root directory.

## Dev setup

Two modes are available:

**Vite Rails (preferred)** — Rails serves the app with Vite proxying assets. Access at `http://localhost-studio.code.org:3000/app`. Requires Rails running (see repo root `README.md`), then from `frontend/`:

```bash
yarn dev
```

**Standalone** — Vite serves the app directly, without Rails. Access at `http://localhost:3036/app`. Useful for frontend-only work that doesn't require a live backend.

```bash
yarn dev   # same command — standalone when Rails is not running
```

Standalone mode is supported by design: Studio is architected to be independently deployable and testable from the backend.

## Routes

Routes are file-based under `src/routes/` using TanStack Router. Read `src/routeTree.gen.ts` or the files in `src/routes/` directly to see current routes — enumerating them here would go stale.

The route tree (`src/routeTree.gen.ts`) is auto-generated by the TanStack Router Vite plugin — do not edit it by hand.

## Adding a lab

See `frontend/docs/conventions/packages.md` (the `## Labs` section) for the full scaffolding steps. The short version — two Studio files must be updated:

- `src/modules/labs/config/labs.ts` — add the lab key to `AVAILABLE_LABS`
- `src/modules/labs/router/getLabEntrypoint.ts` — add the lazy import to `LabEntrypoints`

## Architecture

See [docs/architecture.md](./docs/architecture.md) for the Rails integration chain, init ordering, and lab loading boundary.
