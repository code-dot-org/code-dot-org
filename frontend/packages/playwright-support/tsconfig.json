{
  "extends": "@code-dot-org/lint-config/typescript/tsconfig.vite.node.json",
  "compilerOptions": {
    "baseUrl": ".",
    // DOM types for the in-page page.evaluate closures (src/visual/index.ts);
    // the package's own code runs in node.
    "lib": ["ES2023", "DOM", "DOM.Iterable"]
  },
  "include": ["src"]
}
