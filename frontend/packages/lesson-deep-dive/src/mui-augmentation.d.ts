// The component library's themes module declares MUI's custom variants; the
// build excludes src/dev, so nothing else imports it into the library program.
import '@code-dot-org/component-library/themes';
