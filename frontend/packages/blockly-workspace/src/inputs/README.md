# Input plugins

These will assign custom types (which are specified via `output` and `check` properties on
argument definitions in blocks) to particular routines that render generic SVG paths.

This is used in conjunction with a generic renderer to add these specialized 'notches' to
the sides of blocks to make them stand out.
