// All lab POMs now import directly from '../../shared/urls'.
// This file is kept to avoid breaking any external tooling that might
// reference the path; it can be deleted once confirmed unused.
export {labLevelUrl, flappyLevelUrl} from '../../shared/urls';
