/dist
/node_modules
/coverage
/.nyc_output
/.idea
.DS_Store

# Self-hosted sandbox assets, generated by `yarn setup:world`. The tracked
# service workers (public/*.js) stay committed — only the vendored bundles are
# ignored.
/public/vendor
