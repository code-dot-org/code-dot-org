// Projects handlers — stub. Follow-up PR will implement
// `GET /projects/.../level/...`, `POST /project_commits`, thumbnail PUT,
// extra-links GET, and the featured-project bookmark endpoint.

import type {RequestHandler} from 'msw';

export const projectsHandlers: RequestHandler[] = [];
