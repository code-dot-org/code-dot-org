// Public API for @code-dot-org/teacher-dashboard
// Export all public symbols from this file.

export {TeacherDashboardHome} from './TeacherDashboardHome';
// Default export so the Studio route can `React.lazy(() => import(...))` it,
// matching the pattern other lazy-loaded packages (e.g. music-lab) use.
export {TeacherDashboardHome as default} from './TeacherDashboardHome';
