# Code-dot-org API

This implements all of the backend routes for the site and provides the utility
methods to push and pull data including the generic `HttpClient` fetch wrapper
and the generic `NetworkError` base class.

## Philosophy

This package contains the schemata and route invocations for any and all API
calls. Therefore, the schemata and basic data types for the data are also
contained in this package.
