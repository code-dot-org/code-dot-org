/** Defines a education standard in internal unit data */
export interface LessonsStandardDefinition {
  seeding_key: {
    ['lesson.key']: string;
    ['framework.shortcode']: string;
    ['standard.shortcode']: string;
  };
}
