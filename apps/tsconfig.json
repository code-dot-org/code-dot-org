// This tsconfig is used by your code editor, such as VS Code. 
// It extends tsconfig.build.json, used for building apps.
// This file should only include configuration settings that are
// needed for the IDE experience (such as Intellisense).
// By default, put new settings in tsconfig.build.json.
{
  "extends": "./tsconfig.build.json",
  "include": [
    "./.storybook/**/*.js",
    "./.storybook/**/*.ts",
    "./src/**/*.ts",
    "./src/**/*.tsx",
    "./test/**/*.ts",
    "./test/**/*.tsx",
    // For IDEs, include js and jsx. This allows Intellisense to
    // detect js and jsx files for imports.
    "./src/**/*.js",
    "./src/**/*.jsx",
  ],
}
