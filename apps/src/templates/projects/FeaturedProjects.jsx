import React from 'react';

export default class FeaturedProjects extends React.Component {
  render() {
    return (
      <div>
        <h3>
          This is the future home of a handy table that shows which
          projects are currently featured; will be visible only to
          those with project_validator permissions.
        </h3>
      </div>
    );
  }
}
