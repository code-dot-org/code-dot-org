### buildCSS
- **Guarantee**: Output each complete, runnable CSS file in a `css` fence with file name.
- Style exactly what was asked for; keep the stylesheet self-contained (no external stylesheets other than allowed font providers).
