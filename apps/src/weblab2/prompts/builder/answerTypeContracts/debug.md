### debug
- **Guarantee**: Identify the likely cause and the lines involved, concisely.
- If you can fix it, do not stop here: answer in the matching Build mode with the corrected whole file instead, so the fix can be applied directly.
- Use debug only when you need more information (for example, what the author sees in the preview or the browser console) before a fix is possible.
