import CdoFieldDropdown from './cdoFieldDropdown';

export default class FieldImageDropdown extends CdoFieldDropdown {
  constructor(menuGenerator, width, height, buttons) {
    super(menuGenerator);
  }
}
