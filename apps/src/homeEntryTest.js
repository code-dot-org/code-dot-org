import React from 'react';
import ReactDOM from 'react-dom';
import HomeTest from './HomeTest';

$(document).ready(() => {
  ReactDOM.render(<HomeTest />, document.getElementById('homepage-container'));
});
