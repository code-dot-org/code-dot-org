<div class="netsim-my-device-tab">

  <% if (level.showMetronome) { %>
    <div class="metronome"></div>
  <% } %>

  <% if (level.showPulseRateSlider) { %>
    <h1>Pulse rate</h1>
    <div class="pulse-rate"></div>
  <% } %>

  <% if (level.showBitRateControl) { %>
    <h1>Bitrate</h1>
    <div class="bitrate"></div>
  <% } %>

  <% if (level.showChunkSizeControl) { %>
    <h1>Chunk size</h1>
    <div class="chunk-size"></div>
  <% } %>

  <% if (level.showEncodingControls.length > 0) { %>
    <div class="encoding"></div>
  <% } %>

</div>
