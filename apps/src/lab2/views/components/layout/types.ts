export interface ColumnPanelConfig {
  minWidth: number;
  initialWidth?: number;
  name: string;
}

export interface RowPanelConfig {
  minHeight: number;
  initialHeight?: number;
  name: string;
}
