import {Typography} from '@mui/material';
import React, {FC} from 'react';

const InterventionBox: FC = () => (
  <div>
    <Typography variant="h2">Intervention</Typography>
    <Typography variant="body1">
      Let&apos;s revisit any concepts that were challenging and work through
      them together.
    </Typography>
  </div>
);

export default InterventionBox;
