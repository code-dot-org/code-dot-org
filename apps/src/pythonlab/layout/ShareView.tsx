import React from 'react';

import moduleStyles from './share-view.module.scss';

const ShareView: React.FunctionComponent = () => {
  return <div className={moduleStyles.container} />;
};

export default ShareView;
