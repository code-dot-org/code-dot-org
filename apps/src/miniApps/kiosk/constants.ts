export enum KioskSignalType {
  // Carries the whole screen to draw, replacing whatever is on it.
  SCENE = 'SCENE',
}

export enum KioskElementType {
  BUTTON = 'button',
  LABEL = 'label',
  SLIDER = 'slider',
}
