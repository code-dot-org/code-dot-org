// UI-variant experiments for Sprite Lab in Lab2. Hardcoded for now; these will
// become real experiment flags if/when the variants ship.

// Multi-scene variant: no World tab; a scene dropdown next to the Code tab
// switches between per-scene code workspaces, and a "go to scene" block jumps
// between them at runtime (Play always starts at the default scene).
export const SCENES_UI_VARIANT = true;
