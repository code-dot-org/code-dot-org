import React from 'react';

import {LabProps} from '@cdo/apps/lab2/types';

// Placeholder view - real quiz-taking UI isn't built yet.
const Quiz: React.FunctionComponent<LabProps> = () => {
  return <div>This is a Quiz level.</div>;
};

export default Quiz;
