// TODO (brent) - some of these things (like this one) aren't actually design
// elements and better belong in a different directory

var React = require('react');

var picker = require('react-colorpicker');

var ColorPickerDialog = React.createClass({
  componentWillRender: function () {
    
  },

  render: function () {

  }
});

module.exports = ColorPickerDialog;
