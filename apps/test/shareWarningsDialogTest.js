window.React = require('React');

var assert = require('./util/testUtils').assert;
var ShareWarningsDialog = require('@cdo/apps/templates/ShareWarningsDialog.jsx');

describe('foo', function () {
  it('bar', function () {
    assert(ShareWarningsDialog);
  });
});
