import {assert} from 'chai';
import {OfflineServiceWorker} from 'offline-service-worker';

// TODO, this probably doesn't work. Still need to setup offline unit test running.
describe('offline-service-worker', () => {
  it("can install", () => {
    // TODO Add call to OfflineServiceWorker.install
    assert(false);
  });
});
