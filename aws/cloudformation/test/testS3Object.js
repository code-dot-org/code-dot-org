const S3Object = require("../s3Object");

describe('S3Object', () => {
    describe('handler', () => {
        it('is an untested function', async () => {
            try {
                await S3Object.handler();
            } catch (error) {
                //
            }
        });
    });
});
