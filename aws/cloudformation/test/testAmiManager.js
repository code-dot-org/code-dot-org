const AmiManager = require("../ami-manager");

describe('AmiManager', () => {
    describe('handler', () => {
        it('is an untested function', async () => {
            try {
                await AmiManager.handler();
            } catch (error) {
                //
            }
        });
    });
});
