targets:
  - name:      redshift-reporting
    type:      redshift
    database:  dashboard
    host:      <%= CDO.redshift_host %>
    port:      5439
    username:  <%= CDO.redshift_username %>
    password:  <%= CDO.redshift_password %>
steps:
  - name: student-progress
    queries:
      - name: user-stages
        file: tables/user_stages.sql
